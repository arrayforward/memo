# HESP：高熵数据——6G 的下一个流量形态

> **High-Entropy Supply Protocol** —— 高熵数据流的采集、确权与切片
>
> 版本 1.3 / 2026 · 华为内部战略汇报稿
>
> 本文面向战略决策层，核心结论可直接用于商业判断。

---

## 执行摘要

**问题**：6G 网络能力（Tbps 级带宽、亚毫秒时延、确定性切片）已经就绪，但内容供给侧出现断层。视频、游戏、XR 在 5.5G 时代已接近饱和，AI 合成内容进一步压低骨干网利用率——6G 面临"有路无车"的投资回收风险。

**判断**：下一个填充 6G 管道的流量形态，不是更高清的视频，而是**高熵数据流**——真实世界产生的、不可压缩、不可预生成、必须经网络实时传输的数据。AI 训练需要它，6G 网络需要它，数据源需要变现通道。

**方案**：HESP（High-Entropy Supply Protocol）定位为"世界计算机的输入层协议"，解决三个核心问题：
1. **确权**：谁的数据、从哪来、怎么证明是真的；
2. **定价**：数据对 AI 模型的真实贡献如何量化、如何分润；
3. **切片**：按熵值和实时性分级，与 6G 网络切片一一映射。

**商业价值**：
- **运营商侧**：上行高熵流 + 下行状态流，同一套切片资源双向变现，6G 投资回收周期从"内容驱动"转向"数据驱动"；
- **政企侧**：政府公域传感器、企业产线数据从"成本项"变为"收入项"，数据要素入表有抓手；
- **消费者侧**：用户行为数据确权后持续获得分润，从"被采集"变为"主动供给"。

**落地节奏**：M0（技术闭环）→ M3（市场成形）→ M6（现网对接），预计 3–4 年完成从原型到规模商用。关键依赖：设备可信根（海思安全芯片）、网络侧校验（运营商空口记录）、基准集治理（多源冗余）。

**风险与对策**：统计判别力随生成模型增强而衰减——正因如此，协议将统计信号严格限定为"定价信号"，真实性守门交给物理层（硬件时间戳、空间一致性、无线电空口记录）。这不是残余风险，是设计出发点。

---

## 行业背景与厂商洞察

### 全球数据市场正在经历结构性转变

**AI 训练数据集市场高速增长**。据 Grand View Research 数据，全球 AI 训练数据集市场规模 2025 年约 32 亿美元，预计 2033 年将达到 163 亿美元，年复合增长率 22.6%cite🛠web_search:10#3:~:text=Market size in 2025...USD 3.2 Billion。Technavio 预测 2026–2030 年间该市场将增长 91.2 亿美元，CAGR 高达 28.9%cite🛠web_search:10#6:~:text=USD 9.12 billion...CAGR of 28.9%。亚太地区是增长最快的市场cite🛠web_search:10#3:~:text=Fastest-growing regional market...Asia Pacific。

**中国数据要素市场进入价值释放期**。赛迪顾问报告显示，2025 年中国数据要素市场规模突破 1000 亿元cite🛠web_search:10#8:~:text=2025年市场规模突破1000亿元；数据产品产值规模 2024 年已超 2 万亿元，2025 年预计超 2.3 万亿元cite🛠web_search:10#9:~:text=产值规模已经超过2万亿元...2025年增长15%。国家数据局持续推动数据要素市场化配置改革，市场正从"制度构建期"加速迈进"价值释放期"cite🛠web_search:10#9:~:text=数据要素市场正从"制度构建期"加速迈进"价值释放期"。

**关键趋势**：高质量人类生成内容正在枯竭，合成数据虽快速增长但无法替代真实世界数据。Technavio 指出，"数据稀缺性和高质量人类生成内容的枯竭"是市场的核心挑战之一cite🛠web_search:10#6:~:text=Data scarcity and exhaustion...high-quality human-generated content。企业越来越依赖经许可和同意的数据集，数据溯源追踪需求 paramountcite🛠web_search:10#6:~:text=need for ethical data sourcing...robust data provenance tracking。

### 主流厂商的 6G 共识：从"连接"到"数据"

**Nokia：AI 将驱动 700% 的流量增长**。Nokia《全球网络流量 2030 报告》预测，全球网络流量到 2033 年将增长 5–9 倍，WAN 流量达到 3,280–6,641 EB/月cite🛠web_search:11#1:~:text=Global WAN traffic...3,280-6,641 EB/month by 2033。AI 流量单独预计 2033 年达到 1,088 EB/月，占全球流量的 30%cite🛠web_search:11#13:~:text=AI could represent 30%...all global traffic。Nokia 警告，AI 可能导致"上行数据流量的潜在激增，如果不做好准备，可能压垮当前网络基础设施"cite🛠web_search:11#0:~:text=potential surge in uplink data traffic...overwhelm our current network infrastructure。Nokia CEO Justin Hotard 在 MWC 2026 上表示，现有移动网络已接近容量极限，AI 应用对网络基础设施提出了根本性要求cite🛠web_search:11#17:~:text=existing mobile networks...reaching their capacity limits。

**Qualcomm：6G 是感知、分布式智能和多设备体验的融合**。Qualcomm 将 6G 定义为"能够感知环境、支持分布式 AI 推理、在软件中建模真实世界"的网络平台cite🛠web_search:11#3:~:text=building a network platform...model the real world in software。ISAC（集成感知与通信）和数字孪生是其核心用例——基站不仅能传输数据，还能通过无线电信号观测和解释物理环境cite🛠web_search:11#3:~:text=6G networks can both move data...observing and interpreting the physical environment。Qualcomm 与 T-Mobile 合作，目标 2029 年 6G 商用化，重点验证连接、集成感知和高性能计算cite🛠web_search:11#5:~:text=Qualcomm Technologies and T-Mobile...6G commercialization targeted for 2029。

**NTT DOCOMO：6G 的五大价值中，"NW for AI"是核心**。NTT DOCOMO 明确将"NW for AI"列为 6G 五大价值之一，目标是"超越以人为中心的用例，从 AI 和机器人获得新收入"，通过"最大化 AI 价值：计算、网络与数据"实现cite🛠web_search:11#20:~:text=NW for AI...Maximizing AI Value...computing, network & data。DOCOMO 与 VIAVI 在 MWC 2026 展示 AI 驱动的 6G 网络控制，通过数字孪生和 AI 减少控制开销，系统吞吐量提升达 20%cite🛠web_search:11#7:~:text=reducing control overhead improves...system throughput by up to 20%。此外，DOCOMO 与 NTT 推进 In-Network Computing (INC)，将 AI 和移动网络服务的计算资源整合到网络中，实现端到端质量控制cite🛠web_search:11#15:~:text=In-Network Computing...integrates computing resources for AI and mobile network services。

**Samsung：AI 原生网络**。Samsung 提出将 AI 嵌入整个网络栈的"AI 原生"方法，从物理层降噪到数据链路层动态资源分配，网络从设计之初就具备智能cite🛠web_search:11#9:~:text=embedding artificial intelligence throughout...the entire network stack。Samsung 与 NTT DOCOMO、KT、KDDI、Verizon、SoftBank 等全球运营商建立 6G 研究合作，AI-RAN 已从实验室进入真实网络环境验证cite🛠web_search:11#9:~:text=AI-RAN has moved beyond...real-world network environments。

### 一个共同的盲区

上述厂商的 6G 愿景都指向了同一个方向——**网络需要承载更多来自真实世界的数据**，以支撑 AI 训练、数字孪生、物理 AI 等应用。但有一个关键问题尚未被系统性回答：

> **这些数据从哪来？谁来确权？怎么定价？怎么证明是真的？**

Nokia 看到了 AI 流量的规模，Qualcomm 看到了感知和数字孪生的需求，NTT DOCOMO 看到了"NW for AI"的价值，Samsung 看到了 AI 原生网络的架构——但他们都没有解决**数据供给侧的协议层问题**。这正是 HESP 的定位空间。

---

## 1. 背景：6G 的投资回收困境

6G 网络的能力已经明确：Tbps 级峰值带宽、亚毫秒时延、确定性网络切片。但有一个根本问题没有解决——**跑在网上的内容是什么？**

当前内容形态三条都不占：
- **视频/直播**：可压缩、可缓存、CDN 边缘命中后不再拉动骨干网；
- **游戏/XR**：5.5G 时代已接近体验天花板，6G 的增量价值有限；
- **AI 合成内容**：低熵、可本地复现，AI 越强大，合成内容越充沛，骨干网越空转。

这不是技术问题，是**商业闭环问题**：运营商建好了路，但没有足够的车来跑，投资回收周期会被显著拉长。Nokia 的预测已经给出了警示——如果不找到新的流量形态，AI 带来的上行激增将直接冲击网络容量cite🛠web_search:11#0:~:text=potential surge in uplink data traffic...overwhelm our current network infrastructure。

### 1.1 高熵数据：6G 的"原生内容"

配得上 6G 管道的流量，必须同时满足三条：

| 属性 | 含义 | 为什么现有内容不满足 |
|------|------|---------------------|
| **不可压缩** | 缓存命中后仍能持续拉动骨干网 | 视频可被 CDN 缓存，合成内容可被本地生成 |
| **不可预生成** | 不能由某个节点本地复现，必须经过网络传输 | AI 合成内容可在边缘节点实时生成 |
| **实时发生** | 延迟即失真，需要确定性切片保障 | 视频点播对时延不敏感 |

高熵数据流天然满足这三条：它是真实世界一秒一秒过出来的，不能被快进、不能被跳过、不能事后补拍。一个司机的十年驾龄、一条产线的十年运转，只能被连续经过。

### 1.2 三类数据源，都在"沉睡"

**人（消费者业务）**：
- 场景：智能驾驶舱内的驾驶员行为、XR 环境中的用户交互、陪护场景下的老人行为模式。
- 现状：这些数据要么消失在终端的循环覆盖里，要么被平台免费捡走。用户没有动机产出"分布外"的行为（即模型没见过的东西），平台捡到的永远只是模型已经会的东西——**采集方向就错了**。
- 机会：如果用户能因"提供高熵数据"获得持续分润，供给意愿会显著改变。据 Grand View Research， Automotive 是 AI 训练数据集增长最快的垂直领域之一cite🛠web_search:10#3:~:text=Automotive segment is expanding...autonomous vehicles and advanced driver assistance systems。

**设备（IoT/车联网）**：
- 场景：车载记录仪、家庭摄像头、工业传感器昼夜观测物理世界。
- 现状：它们是哑终端——能看，不能说；说出来的，没人确权，没人付费。
- 机会：接入 HESP 后，设备数据可直接变现，从"成本项"变为"收入项"。

**公共与企业存量（政企业务）**：
- 场景：政府公域传感器（交通、气象、河道）、企业产线记录与调度日志。
- 现状：数据本来就在产生，本来就在消失——不是谁刻意浪费，是缺一个接住它们的地方。
- 机会：接进来就是财政收入通道与企业收入来源，**数据要素入表**有明确抓手。中国数据交易市场规模 2025 年约 2046 亿元，2030 年预计达 5156 亿元cite🛠web_search:10#0:~:text=数据交易市场规模（2030E）...5156亿元。

### 1.3 付费方是谁？

最需要高熵数据的一方，恰好也是付费能力最强的一方——**AI 产业**。

全球 AI 训练数据集市场 2025 年约 32 亿美元，2033 年预计达 163 亿美元cite🛠web_search:10#3:~:text=Market size in 2025...USD 3.2 Billion。但这个数字只统计了"被交易的数据集"，没有统计企业自采、政府公域、设备日志等**未被定价的沉睡数据**。如果把这些数据接进来，市场规模的上限会显著高于当前预测。

而这个需求的终点不是 AI 自己——**AI 需要高熵信息源，是因为人要 AI 更好地服务人**。数据从人处来，价值回到人处去。

**三方同向**：AI 得到训练数据，6G 得到流量，数据源得到报酬。缺的是让这件事可持续的机制。

---

## 2. 方案定位：HESP 是什么、做什么、不做什么

### 2.1 三层架构

数据流被接进来之后，并不是到 HESP 为止。它往上还要经过两层，HESP 的形状由它们决定。

```
┌─────────────────────────────────────────────────┐
│ 输出层：订阅 / 呈现 / 具身在场                     │  ← 事实 → 在场
├─────────────────────────────────────────────────┤
│ 内核：自治域 / 决策环 / 一次结算即成事实            │  ← 信号 → 事实
├─────────────────────────────────────────────────┤
│ 输入层：HESP（本协议）采集 / 确权 / 切片            │  ← 真实 → 机器
└─────────────────────────────────────────────────┘
```

- **输入层（HESP）**：负责流的来路——谁产的、真不真、值多少。
- **内核**：负责流的去处——信号如何成为事实。物理世界被切成若干自治域（一辆车、一个路口、一座港口），传感器的高频样本经迟滞状态机过滤，跨过迁移沿的才进入决策环，结算为 FACT。
- **输出层**：负责流的回声——事实如何被看见。按需订阅，无人订阅的实体不耗算力。

三层共用一条脊椎：append-only 的事实档案与同一套网络切片资源。

### 2.2 HESP 的边界

HESP 与邻居层的接口只有两个：
- **感知接口**（向上）：验证合格的包交给内核；
- **切片接口**（横向）：与输出层共享同一网络切片资源池。

HESP 不承诺包一定成为事实（内核决定"算不算数"），内核不承诺包的真实性（HESP 决定"是不是真的"）。**输入层保证"这是真的"，内核决定"这算不算数"**。

---

## 3. 四条不变量（协议的"宪法"）

协议的全部主张压缩为四条。任何新机制提案、任何争议，先问它与哪条不变量冲突；不冲突的，协议无权干预。

### I. 真实性只锚定在物理层

能判定数据流真伪的证据只有三类：出处链完整、物理层痕迹存在（硬件时间戳签名、空间一致性、无线电空口记录）、时间深度可验证。

一切统计判别——熵值估计、行为指纹、分布亲缘——是**定价信号**，只影响价格，永远无权拒绝登记或给数据定罪。统计判别随生成模型增强必然失效，所以它不守门；守门的东西必须不随模型能力退化。

**时间连续性公理**：行为流是依赖深度无界增长的链式状态。伪造第 N 天的流 = 发明一部与前 N−1 天全部咬合的历史，维持成本随时间超线性上升，而诚实采集成本恒定，两条成本曲线必相交——交点之后伪造必亏。协议不声称能证明时间真实，只声称：在公理成立的前提下，伪造连续时间的成本 ≥ 诚实采集的代价。

### II. 价值主张必须可重跑

任何定价与归因主张，必须附带可复现实验包（随机种子、数据哈希、执行环境、结果全登记）；任一方可重跑验证。规则可以被博弈，实验可以被重跑。

### III. 对手能力一次性声明

协议假设对手：持有真实数据流、任意算力、可与至多一个内部角色合谋。协议的真实性主张在此对手模型下一次说清，不随攻击花样逐条回应。

### IV. 协议只管证据，不管行为

不规定价格，不规定授权如何取得，不规定市场结构，不惩罚任何人。只生产证据：append-only 记录、可复现实验、物理层采样点。隐私、同意、分润公平、反垄断，由法律与市场依据协议生产的证据裁决。法律负责裁决，技术负责举证。

---

## 4. HES Packet：最小完整单元

HES Packet 是一次可路由、可验证、可切片的高熵数据流的最小单元，一切确权与计价只以包内声明为准：

```
┌──────────────────────────────────────────────────────┐
│ magic / format_version / packet_id (128-bit)          │
│ source_type      : human | device | public | enterprise│
│ topic_embedding / drift_type / required_time_scale    │
│ patch_class      : S | G | E                           │
│ entropy_estimate : bits/s（定价信号，非真伪证据，I）    │
│ realtime_class   : hard | soft | offline               │
│ slice_requirement: S0 | S1 | S2 | S3                   │
│ bandwidth_profile: { peak_bps, mean_bps, duration_s }  │
│ cost_meta        : 代价元数据（按源类型的强制字段集）   │
│ consent_scope    : 授权范围声明（空或越界即无效包）     │
│ raw_content_offloaded : 出端形态（统计摘要｜原始内容）  │
│ provenance_chain : append-only 出处链                  │
│ delta_payload    : 数据流本体                           │
│ label + confidence（归类器输出，可追加 LabelCorrected） │
│ issuer_signature / hes_signature（哈希链＋签名）        │
└──────────────────────────────────────────────────────┘
```

`cost_meta` 公共项：`hardware_ts_sig`、`duration`。按源追加：

| 源类型 | 强制追加字段 | 华为侧映射 |
|--------|-------------|-----------|
| human | `interrupt_count`、`hesitation_rt_dist`、`exit_possible` | 鸿蒙座舱交互、XR 手柄、可穿戴设备 |
| device | `device_attestation`、`uptime_profile` | 海思安全芯片 attestation、IoT 设备 |
| public | `operator_attestation`、`asset_registry_ref` | 运营商空口记录、政府资产登记 |
| enterprise | `entity_attestation`、`asset_registry_ref`、`data_profile` | 企业 ERP/MES 系统、产线传感器 |

缺所属 profile 任一字段的包视为无效包。

`raw_content_offloaded` 为出端形态声明，与 `consent_scope` 一致才有效；`hes_signature` 为链式签名，断裂即拒绝登记。

---

## 5. 需求登记、采集与供给生命周期

### 5.1 需求侧：盲区地图

"更好地服务人"是抽象目标，盲区地图是把它变成具体数据需求的转换器。

**服务盲区 → 服务质量提升**：
AI 在服务中力有不逮之处，就是盲区。模型方维护对自身不确定性的持续登记（盲区地图，模型方私有），据此向数据源签发请求——例如：
- "山城夜间雨雾中的窄路会车"（自动驾驶）
- "在 XR 环境中连续使用一个月，记录全部不适与即兴决策"（消费者业务）
- "高温高湿环境下连续 72 小时产线电流波动"（智能制造）

**现实漂移 → 世界模型保鲜**：
树被砍、店换招牌、产线工艺漂移——现实在流变，模型不更新即腐烂。变化巡检持续扫描变化信号，命中即立项采集。

盲区地图有个逻辑极限：它只能覆盖"已知的未知"。所以供给天然双层：被请求的供给由盲区地图引导，未被请求的供给由真实世界的探索行为产生。**两条供给路径在协议内同权**，市场按实际贡献给两者定价。

### 5.2 带宽分级（切片即定价变量）

补丁的熵值与实时性映射切片等级——切片不是传输选项，是定价变量：

| 级别 | 定义 | 切片 | 典型场景 | 商业类比 |
|------|------|------|----------|----------|
| Patch-S | 单帧观察＋时间戳，保鲜级 | S0/S1 | 海量低峰连接，给 6G 打底 | "基础流量包" |
| Patch-G | 策略对抗行为流，博弈级 | S2 | 玩家、红队钻出的未覆盖路径 | "增值服务包" |
| Patch-E | 长期全息委托，前沿级 | S3 | 一座自动化港口的 Gbps 级十年委托流 | "专线级 SLA" |

### 5.3 供给状态机（含退出即信号）

```
IDLE → LOCKED → ENGAGED ──→ SETTLED
           │        │
        E1 离线  E2 退出 / E3 挑战失败
           ▼        ▼
        Exit_Answer → 按已有轨迹正常结算
        E3 → ADVERSARIAL（转欺骗策略货架计价，不拒绝、不删除）
```

ENGAGED 内部叠加连续量 `engagement_gradient`（中断率、带宽衰减、犹豫漂移的日度聚合斜率，最近 14 天回归斜率＋EWMA 半衰期 7 日平滑）：梯度下行即渐退，协议自动逐级降切片（S3→S2→…），渐退期流照常计价——**"源是怎样慢慢退出的"比"源退出了"信息量更大**，退出前奏标注为一等高熵信号。退出理由哈希封存，登记即解锁，按已有轨迹正常结算。

### 5.4 归类器与误报控制

**归类器输出** `(label, confidence)`：`genuine` / `performed` / `adversarial` / `unknown`（转探索货架，低价入库不丢弃）。标签概率化、可追加 `LabelCorrected`（历史不改写，差价补付）。归类器代码与基准数据集公开；归类器只影响价格（I），无权拒绝包。

**误报控制**（强制，针对 human 源）：

1. **阈值个性化**：`hesitation_rt_dist` 的偏离阈值 $	heta_h$ 随个人基线方差自适应，参考取个人基线期自助分布的 99 分位；
2. **持续性要求**：单窗越限只标记"观察"，连续 3 窗越限才升级；
3. **双信号规则**：单一行为信号不得置 `adversarial`，须 ≥2 个独立信号协同印证。人类反应时随年龄、疲劳的自然漂移由前两层吸收，真实人被一信号定罪的路径由第三层关闭。

### 5.5 hesitation_rt_dist 参考实现

对每次交互事件记录反应时 $rt_i$（硬件时间戳时钟源），窗口取"最近 30 天"与"最近 200 个样本"中先满足者，输出分位数 `[p5, p25, p50, p75, p95]`、20 桶直方图香农熵 $H$、样本数 $n$。基线取会话最初 14 天，EWMA（$lpha = 0.05$）慢速更新；偏离度量 $D = W_1$（当前窗口, 基线）。

---

## 6. 硬编码（协议级不可配置）

以下 20 条为协议级断言，任何部署实例必须满足，不可通过配置覆盖：

| 断言键 | 值 / 条件 | 语义 |
|--------|-----------|------|
| `records.append_only` | `true` | 记录即事实：不删除，只追加 |
| `provenance_chain.mutable` | `false` | 出处链只增不改；篡改按最高优先级安全事件 |
| `consent.required` | `true` | `consent_scope` 空或采集越界即无效包 |
| `consent.revocable` | `true` | `Consent_Revoked` 即时生效；历史不删，已结算不追回 |
| `privacy.raw_content_offload_scope` | `"consent_declared_only"` | 出端形态以 `consent_scope` 为边界 |
| `request.no_identity_targeting` | `true` | 最小化：请求方不得指定与盲区无关的个人身份属性 |
| `statistical_signals.pricing_only` | `true` | 统计判别只是定价信号（I） |
| `synthetic_content.slice_premium` | `0` | 合成内容不享切片溢价 |
| `classifier.code_and_gold_set_public` | `true` | 归类器代码与基准集公开 |
| `ablation.granularity` | `"patch_set_only"` | 单份流不可归因，补丁集可归因；消融定总价，集内按收购价分割 |
| `stale_threshold` | `0.005 × gold_set_score` | 「趋零」阈值硬编码为基准集消融得分的 0.5% |
| `stale_invalid.threshold` | `3` | 连续 3 次低于阈值 → 停止分润 |
| `stale_revival_window` | `180d` | 保鲜无效复活窗口 180 天 |
| `ablation_L1.min_sample_gate` | `true` | 样本量不足时 L1 只输出「观察中」，不输出归因权重 |
| `adversarial.min_independent_signals` | `≥ 2` | 双信号规则（FPR 控制） |
| `hardware_ts.cert_expiry_grace` | `7d` | 证书过期走降级通道（溢价折减），不等同于伪造拒绝 |
| `settlement.consumes` | `"endpoint_labels_only"` | 结算层只消费端点标签，不解读内容 |
| `bystander_royalty.auto_transfer` | `true` | 被观察者分润自动划转义务不可绕过 |
| `deployment.trust_assumption_acceptance_registered` | `true` | 网络层校验启用前须登记信任假设接受声明 |

隐私出端形态、被观察者权益、费用分摊、市场结构——均为实现层与法域事务，协议只提供可审计接口，不进硬编码。

---

## 7. 层间接口

### 7.1 感知接口（HESP → 内核）

验证合格的 HES Packet 投递到目标域后，走内核的标准入口：

1. `delta_payload` 写入该域**感知日志**（滚动缓冲，非真源，可丢弃）；
2. 过迟滞状态机与聚合网关——只有跨过迁移沿的才成为候选；
3. 进决策环：沙盒预演，通过则结算为 FACT，写入域事件日志；
4. HESP 侧的 `packet_id` 与内核侧 FACT 的 `eventId` 互记因果指针——**确权链（HESP）与事实链（内核）在此缝合**：定价沿确权链追溯到包，定责沿事实链追溯到事件，两条链各自单调可读。

### 7.2 孪生保鲜接口（HESP ↔ 内核）

内核与输出层靠惰性维持算力可行：无人订阅的实体与区域沉睡，不耗算力。但沉睡的现实在流变——树被砍、店换招牌、河道改道，镜像不维护即腐烂。

HESP 的分布式源是连续驻场的：
- `topic_embedding` 与镜像区域同嵌入空间，Patch-S 流可直接唤醒对应沉睡区域；
- 唤醒后生成的状态须不与已结算的感知事实矛盾，HESP 流构成这些约束的一部分；
- 一份保鲜流可同时供给模型方（训练）与内核（镜像维护），两次计价互不冲突——**上层的核心成本项，就是输入层的核心收入项。**

这与 NTT DOCOMO 的"Self-awareness Network"概念同源：利用数字孪生和 AI 评估网络质量，实现实时自适应cite🛠web_search:11#7:~:text=Self-awareness Network...AI and digital twin technologies。HESP 提供的是数字孪生所需的**真实世界数据源**。

### 7.3 切片接口（HESP ↔ 网络，与输出层共享）

输入层的 S0–S3 与输出层的订阅分级共享同一网络切片资源池：上行高熵流按熵值与实时性计费，下行状态流按体验颗粒度计费。**带宽第一次同时向两个方向变现**，中间是同一套切片资源。S0–S3 为协议内逻辑命名，与任何外部标准标识的映射由接入网适配层完成——标准组织改了编号，本协议一个标点不用动。

### 7.4 事实档案接口（三层共用）

HESP 的权属与流水记录、内核的域事件日志、输出层的交互事实，写入同一种 append-only 归档：哈希链＋签名＋因果指针，最终一致复制，只存哈希不存载荷。全系统不存在跨层强一致部件；终局性在源处成立的一刻已确定，归档只是公证。

---

## 8. 信任假设（显式声明）

协议不假装"去信任"，只标注信任边界：

**0. 时间的连续性（公理）**

任何有物理承载的数据流，其时间维度连续、不可逆、不可压缩——十年只能被连续经过。机制支点见第 3 章 I 的附注（二阶链→依赖深度→成本曲线相交）。这是全部真实性主张的支点，无法在协议内证明，只能显式声明。

**1. 设备可信根**

硬件时间戳签名的信任根是安全芯片厂商密钥；缓解为多厂商设备多样性＋网络侧交叉比对。

**2. 基准集治理**

归类器基准数据集的托管与抗腐化是协议外工程；缓解为多源冗余＋交叉互验。

**3. 运营商侧可信根**

网络侧校验的信任根是运营商自身的空口记录；运营商与采集商合谋时，协议内无独立第三方物理测量通道。部署前置：启用网络层校验定价效力前须登记信任假设接受声明（见第 6 章）。

**4. 模型方绕过**

协议只能改变绕开协议自行获取数据的相对成本，不能消灭绕过。

**5. 统计判别力衰减**

全部统计信号随生成模型增强而衰减——正因如此它们只是定价信号（I）。这不是残余风险，是设计的出发点。

---

## 9. 消融、保鲜与结算（机制摘要）

### 9.1 消融预言机（可复现审计），分层结构

- **L1 常规层（例行）**：代理模型＋子采样回归，输出归因排序与置信区间；样本量低于 $n_{min}$（参考 200）时只输出「观察中」，分润挂起累积。
- **L2 质疑层（挑战触发）**：微调式留一法复验（基座固定，用/不用各微调一次）；挑战方预付成本，失败不退。
- **L3 终裁层（罕见）**：真·双世界全量消融，仅限 L2 无法收敛且争议标的超阈值时触发，结果构成终局证据。其存在本身即威慑。

全部实验的随机种子、数据哈希、执行环境与结果按事实终局登记；任何第三方可复现重跑——可复现性即防腐。

### 9.2 保鲜无效流程

1. 某补丁集连续 3 次 L1 消融贡献低于 $0.005 \times$ 基准集消融得分 → 自动打 `stale_invalid`，停止分润，衰减率登记公开。
2. **180 天复活窗口**：若出现新消融证据（下游模型在该集上恢复正向迁移）→ 预言机重新验证 → 打 `stale_revived`，恢复分润。
3. 逾期未复活 → 永久封存，打包进 `.quipu` 档案（不再参与分润）。

`stale_invalid` 不删除数据、不追回历史、数据仍可用于推理（仅停止分润）。

---

## 10. 落地路径与里程碑

| 阶段 | 时间预估 | 验收标准 | 华为侧抓手 | 行业对标 |
|------|----------|----------|-----------|----------|
| **M0** 最小闭环 | 6–9 个月 | 盲区不确定度随轮次下降；注入已知价值补丁，价值排序与真值 Spearman $ho \geq 0.6$ | 昇腾集群 + 内部模型验证 | — |
| **M1** 真实接入 | 9–12 个月 | 真人数据源＋真实小模型，价值排序与人工评估 $ho \geq 0.6$（预注册规程） | 鸿蒙座舱/可穿戴试点 | — |
| **M2** 内核对接 | 12–18 个月 | HES Packet → 感知日志 → 决策环全链路打通；`packet_id` ↔ `eventId` 因果指针可双向审计 | 与车 BU/云核对接 | — |
| **M3** 市场成形 | 18–24 个月 | 多源供给、多方采购，价格信号有效；供给方获得持续偿付 | 运营商联合试点（1–2 省） | Nokia 预测 AI 流量 2033 年达 1,088 EB/月cite🛠web_search:11#0:~:text=AI traffic is expected...1088 exabytes (EB) per month by 2033 |
| **M4** 长期委托 | 24–30 个月 | 年际全息流接入；退出事件照常计价；孪生保鲜唤醒链路打通 | 政企标杆项目（港口/工厂） | NTT DOCOMO 目标 2030 年 6G 商用cite🛠web_search:11#20:~:text=aiming for the service realization around 2030 |
| **M5** 分润与保鲜 | 30–36 个月 | 消融归因结算；保鲜无效标签生效；复活窗口分支可复现 | 财务/法务流程跑通 | 中国数据交易市场规模 2030 年预计 5,156 亿元cite🛠web_search:10#0:~:text=数据交易市场规模（2030E）...5156亿元 |
| **M6** 现网对接 | 36–48 个月 | 单源 Gbps 级全息流 72 小时不间断传输＋S3 切片计费全链路；网络层交叉验证定价效力启用 | 现网规模商用 | Qualcomm-T-Mobile 目标 2029 年 6G 商用cite🛠web_search:11#5:~:text=6G commercialization targeted for 2029 |

### 零点启动（冷启动构件，全部 append-only 登记、版本化演进）

- **G0** 初始盲区地图由 ≥3 个相互独立的开源模型委员会贡献；
- **G1** 初始基准集由 ≥3 个独立学术数据集锚定；
- **G2** 双轨演进；
- **G3** 信任收敛——最初的可信来自公共学术共识，最终的可信由经校验的长期真实流接管。

**启动构件是脚手架，不是地基。**

---

## 11. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| 统计判别力随生成模型增强而衰减 | 归类器误报率上升，定价信号失真 | **已设计入协议**：统计信号严格限定为"定价信号"，不用于真伪判定；真实性守门交给物理层（I） |
| 设备可信根单点故障 | 硬件时间戳签名被攻破，整条链失效 | 多厂商设备多样性＋网络侧交叉比对；海思安全芯片作为可信根之一，不唯一依赖 |
| 运营商与采集商合谋 | 空口记录被篡改，网络侧校验失效 | 协议内无独立第三方物理测量通道，这是已知边界；缓解为部署前强制登记信任假设接受声明 |
| 模型方绕过协议自行采集 | 协议生态被架空 | 协议改变的是"相对成本"而非"绝对可行性"；当协议内分润 > 绕过成本时，理性参与方会选择协议 |
| 基线期污染（用户前 14 天即表演） | `hesitation_rt_dist` 阈值失效 | 基线期内若触发 adversarial 信号，自动延长基线采集期或标记为「可疑基线」 |
| 用户隐私与合规（GDPR/个保法） | 监管风险 | 协议不解读内容，只消费端点标签；`consent_scope` 为空或越界即无效包；`Consent_Revoked` 即时生效 |

---

## 结语

HESP 为 6G 扩展流量——把真实世界的高熵数据流接进来，可验证、可定价、可审计：AI 得到训练数据，6G 得到流量，数据源得到报酬。数据从人处来，价值回到人处去：人供给真实，AI 服务人类，协议让这件事可持续。

**管道铺在这里，水会来。**

---

## 附录：参考来源

1. Grand View Research, *AI Training Dataset Market Size & Share Report, 2026-2033*, 2026.
2. Technavio, *AI Training Dataset Market Size 2026-2030*, 2026.
3. 赛迪顾问, *2025年中国数据要素市场研究报告*, 2026.
4. 国家数据局 / 中国发展高层论坛 2026, 数据产品产值规模数据.
5. Nokia, *Global Network Traffic Report 2030*, 2024/2025.
6. RCR Wireless News, *Qualcomm sees 6G commercialization being shaped by sensing, distributed intelligence...*, 2026.
7. NTT DOCOMO, *5G Evolution & 6G White Paper*, 2025/2026.
8. NTT DOCOMO / VIAVI, *AI-Driven Network Control For 6G Demonstration*, MWC 2026.
9. Samsung Newsroom, *Samsung Leads 6G Revolution With AI-Native Networks*, 2025.
10. 锋行链盟, *中国数字资产发展报告（2025-2026）*, 2026.

---

*最后更新：2026-08-13 · 华为内部战略汇报稿*
