# 投影域游戏架构设计文档

> 版本：v1.0
> 定位：本文档是一套全新游戏底层架构的完整设计，用于指导 AI 或工程团队从零构建系统。
> 阅读顺序：先读第 1、2 章确立世界观与公理，后续章节均为其工程展开。

---

## 1. 设计哲学：一人一世界

### 1.1 核心世界观

本架构的第一性原理：**游戏不是模拟一个世界然后给人看，而是为每个玩家排演一个属于他的世界。**

- 每个玩家拥有一个永恒的、私人的域（Domain），以他自己的心跳运行。他是域内唯一的"真人"。
- 域内其余一切皆为**投影（Projection）**：NPC 是服务器扮演的投影，其他玩家是真人驱动、经服务器路由而来的投影。两者在呈现协议上完全相同，区别仅在故事来源。
- **不存在"世界"这个实体。** 存在的只有三样东西：纯数学的索引（货架）、玩家各自的域（摄影棚）、服务器撰写的正史（档案）。玩家感受到的"共同世界"，是所有剧本被同一部正史校准后的产物。
- 类比：每个玩家都是楚门，看到的是为他排演的世界；玩家相遇时不需要合并摄影棚，只互相在对方的棚里投放投影。

### 1.2 三条元原则

1. **叙事优先于真实。** 系统交付的不是"客观真相"，而是"每个玩家都能接受的、自洽的因果故事"。绝对的同时性与绝对的公平在物理上不存在，架构不追求它们，而是用规则和延迟窗口收编模糊性。
2. **已呈现不可推翻。** 玩家已经看到的因果不得被系统作废（Rollback 是最后手段而非常规机制）。规则设计必须保证：绝大多数交互的结果在双方各自的故事里都成立。
3. **存在即被感知。** 无观察者的实体退化为潜在（索引条目 + 极低频心跳），模拟算力正比于被观察密度。世界的"存在"随观察而生。

### 1.3 与传统架构的根本差异

| 维度 | 传统架构（心跳内核） | 本架构（投影域） |
|---|---|---|
| 世界 | 一个共享世界，全局心跳 | 不存在世界，一人一域 |
| 时间 | 全局统一逻辑时钟 | 每个实体自有心跳，无全局时钟 |
| 仲裁 | 服务器计算真相，全局裁决 | 只在交互点生成"正史事件"，局部且终局 |
| 玩家相遇 | 天然同处一个世界 | 互相投放投影，订阅关系临时建立 |
| 状态 | 全量快照，逻辑帧 | 实体各自的时间线（ring buffer），故事流 |
| 客户端 | 世界的窗口（可作弊） | 剧本的观众（架构性防作弊） |
| 性能上限 | 全局心跳周期 × 实体总数 | 随玩家戏剧密度线性分布，无全局上限 |

---

## 2. 架构公理（不可违背的十条）

以下公理是系统的宪法。任何实现决策与公理冲突时，以实现让步。

1. **纯数学底座**：系统最底层只有坐标系与索引，无实体、无心跳、无计算。
2. **实体自有心跳**：石头一年一次，人一秒 20 次，心跳随活跃度与观察密度自适应。无全局 barrier。
3. **故事是唯一流通物**：实体间只交换"故事"（带信封的不透明字节流），底层不理解内容。
4. **端到端语义**：语义只存在于端点（实体与渲染器），传输层只管送达、有序、不篡改。
5. **一人一域，域不合并**：每个玩家永远是其域内唯一真人，他人皆为投影。订阅替代合并。
6. **三级参与**：实体间关系分三级——订阅（收故事）→ 同步（共享时钟）→ 仲裁（参与裁决）。默认停在最低级，成本逐级跳升。
7. **正史终局**：交互结果由服务端实体一次结算即成正史（结果直送为常态、临时仲裁为例外），双方域各自呈现，不得回溯改写。
8. **规则可交换化**：凡时序模糊下结果对立的交互，规则上必须允许双生效（双杀）、偏袒行为者或宽限窗口，使顺序无关。
9. **惰性模拟与惰性呈现**：未被订阅的实体不模拟；未被观看的细节不呈现。可证伪性（与玩家记忆不矛盾）是模拟的唯一约束。
10. **分层依赖不可逆**：慢层（数学/传输）不依赖快层（玩法/表现）。任何反向依赖即架构腐烂的开始。

---

## 3. 系统分层总览

```
┌─────────────────────────────────────────────┐
│ L4  呈现层（玩家侧）                          │
│     渲染实体 = 故事的听众，把故事演成画面        │
├─────────────────────────────────────────────┤
│ L3  玩法规则层（品类相关，数据驱动）            │
│     技能/任务/经济 = 方言 + 原语装配            │
├─────────────────────────────────────────────┤
│ L2  导演层（服务器侧）                        │
│     NPC投影扮演、事件编排、离线补史、正史对齐    │
├─────────────────────────────────────────────┤
│ L1  实体层                                   │
│     自有心跳的进程、时间线 ring buffer、正史裁决 │
├─────────────────────────────────────────────┤
│ L0  传输层                                   │
│     信封路由：送达、域内有序、不篡改             │
├─────────────────────────────────────────────┤
│ L-1 纯数学层                                  │
│     坐标系 + 空间索引 + 关系索引（社交图）       │
└─────────────────────────────────────────────┘
```

关键性质：**L-1 与 L0 不含任何"游戏"概念**。几何、网络、进程模型——游戏完全生长在 L1 以上的语义层里。这两层的代码保质期以"永久"为目标。

---

## 4. L-1 纯数学层

### 4.1 职责

只回答一个问题：**"故事该流向谁？"** 不计算、不推进、不仲裁。

### 4.2 三大构件

1. **坐标系**：支持多尺度（恒星系级到毫米级）的空间定位。实现建议：层次化网格/树结构（八叉树、27 叉树或锚点层次），索引键为多尺度坐标编码。
2. **空间索引**：判定 `|pos(a) − pos(b)| < r`，输出"可能产生感知关系的实体对"候选集。支持动态实体增删与高频移动。
3. **关系索引（社交图）**：判定 `edge(a, b) ∈ 社交图谱`（好友/队伍/公会/仇敌/关注）。与空间索引并列，输出同一种东西："这两个实体应该收到彼此的故事"。

### 4.3 两条感知通道

| | 通道 A：空间通道 | 通道 B：关系通道 |
|---|---|---|
| 触发 | 空间邻近 | 社交图谱边 |
| 性质 | 对称、匿名、临时、被动 | 可不对称、实名、持久、主动 |
| 带宽 | 随距离衰减 | 由关系强度决定，与距离无关 |
| 成本 | O(空间密度) | O(关系数) |

**运营含义**：MMO 生命周期 = 通道 A 制造碰撞 → 转化为通道 B → 通道 B 锁定留存。服务器健康度指标 = 通道 B 承载的故事流量占比。

### 4.4 约束

- 本层不知道"玩家""NPC""技能"等任何概念。
- 索引只做候选粗筛，不做精确判定（精确判定属 L1）。

---

## 5. L0 传输层

### 5.1 信封模型

故事 = 信封 + 不透明负载：

```
Envelope {
    dialect:  DialectId     // 方言标识 + 版本号（接收方据此选择解释器）
    from:     EntityId      // 讲述者
    channel:  ChannelId     // 所属通道（空间域/关系域/私域）
    stamp:    发送方局部时间戳 // 排序原料，非全局时钟
    payload:  opaque bytes  // 传输层永不打开
}
```

### 5.2 传输承诺

- **送达**（可靠通道）或**尽力送达**（高频可丢通道，按数据画像选择）；
- **通道内按序**；
- **不篡改**（完整性校验）。

### 5.3 方言（Dialect）

- 每种故事类型（移动、战斗、外观、聊天……）是一种方言，有独立 ID 与版本。
- 方言是**端点间的双边私约**，传输层无感知。新方言 = 新品类，无需改动 L-1/L0。
- 实体相遇时先交换方言能力表（capability negotiation），不认识的方言可降级为忽略或通用 fallback。
- 方言版本滚动升级，新旧版本故事可在同一通道共存。

### 5.4 数据画像驱动的通道选择

每类故事声明画像元数据（因果性、时效性、可丢失性、空间相关性、频率），传输层按画像分流：

- 高频连续量（移动）→ 不可靠有序通道，允许丢包，只要最新；
- 正史事件（命中、死亡、交易）→ 可靠有序通道，必达；
- 表现性内容（特效参数）→ 最低优先级，可整体丢弃。

---

## 6. L1 实体层

### 6.1 实体模型

每个实体是一个独立逻辑进程：

```
Entity {
    id, 空间位置(注册在 L-1 索引)
    heartbeat: 自有心跳频率（自适应）
    timeline:  时间线 ring buffer [(t, state), ...]
    inbox/outbox: 故事收发队列
    dialects:  我会讲/能听懂的方言表
}
```

### 6.2 心跳自适应律

- 基础心跳由实体类型决定：玩家 20Hz，普通 NPC 1–5Hz，山/石头 ≈ 0（事件唤醒）。
- 修正因子：被观察密度 ↑ → 心跳 ↑；交互烈度 ↑ → 心跳 ↑；远离一切观察者 → 心跳 → 0（沉睡）。
- 沉睡实体在索引中保留条目与最后状态，被接近/被引用时唤醒。

### 6.3 时间线（Timeline）

- 每个实体维护自身状态历史：环形缓冲，采样点 = 心跳点。
- 时间线同时服务三件事：碰撞判定（扫掠轨迹）、呈现插值（对观察者输出）、正史留痕（战报/回放）。
- 压缩策略：长时间未观察的历史可压实为摘要快照（"三天前他在打铁"），细节丢弃但不得与任何玩家记忆矛盾。

### 6.4 交互的三级处理：仲裁是例外，路由是本体

**关键原则：服务器最关键是路由，不是仲裁。** 玩家实体运行在服务器上，其业务结算结果天然可信、天然权威。因此绝大多数交互无需任何仲裁：

```
Level 0 结果直送（绝大多数交互）
  A 的服务端实体本地结算（伤害、Buff、位移），一次计算即成正史
  → 结果故事直接路由给 B
  → B 的实体不重复计算：接收、应用、呈现
  （A 即权威，因为它跑在服务器上，而非客户端上）

Level 1 规则消解（冲突但可交换化，少量）
  时间窗口内双方互相出手，两份结果故事语义冲突
  → 路由节点上的静态规则直接消解（如双杀 = "两份都成立，都投递"）
  → 消解逻辑是纯函数、无状态、可配表，不构成"裁决过程"

Level 2 临时仲裁（强耦合，极少数）
  P3 同步域（格斗/投技）内双方逐帧互相决定
  → 临时拉起仲裁上下文，交互结束即销毁
```

正史的定义因此修正为：**"第一个成立的结果故事"即为正史**——大部分正史由实体自己书写，服务器史官只负责保管与对齐，不负责撰写。正史一旦成立即终局，不回滚、不改写。规则的验收标准见第 8 章。

### 6.5 权威与反作弊

- 所有实体运行在服务器上：B 信任的不是"A 的客户端"，而是"A 这个服务端实体的输出"。权威内生于部署形态，无需独立裁决层维持。
- 客户端只是观众：只发送输入意图，收到的只是剧本。
- **未订阅的实体，其故事不下发**——透视类外挂无数据可用（架构性免疫）。
- 不设独立的反外挂层：输入合法性由实体自身的业务规则校验，结算合法性由"实体在服务器上"天然保证。

---

## 7. 投影域：一人一世界

### 7.1 域的结构

```
PlayerDomain {
    owner: 玩家实体（域内唯一真人，最高心跳优先级）
    projections: 投影列表 = 我订阅的一切
        ├─ 真人投影（其他玩家，故事经服务器路由）
        └─ 扮演投影（NPC/怪物，服务器导演驱动）
    localClock: 域内时钟（跟随主人的心跳节奏）
    stage: 呈现编排器（把故事演成体验）
}
```

### 7.2 相遇即订阅

- 空间通道：两个玩家的感知半径重叠 → 互相在对方域中创建投影，临时订阅建立。
- 关系通道：好友/队伍成员 → 持久订阅，相隔万里也同频（低带宽状态流 + 重要事件推送）。
- 订阅分级（投影质量）：

| 级别 | 触发 | 数据内容 | 更新频率 |
|---|---|---|---|
| P0 雕像 | 视野极远/列表中 | 位置 + 外观摘要 | 秒级 |
| P1 剪影 | 远距可见 | 位置插值 + 基础动作 | 1–5Hz |
| P2 全彩 | 近距/交互中 | 完整状态 + 导数（速度/朝向） | 10–20Hz |
| P3 同步 | 强耦合交互（格斗/投技） | 输入级同步 | 逐帧 |

P3 是"临时共识域"的唯一保留形态：仅当两个玩家进入亚 100ms 级互相决定的强耦合（格斗投技、贴身缠斗）时，订阅升级为同步，交互结束即降级。**除 P3 外，本架构不存在域合并。**

### 7.3 交互闭环（以攻击为例）

```
1. A 域：A 对 B 的投影出手 → 意图上报服务器上 A 的实体
2. A 的实体本地结算（Level 0，无仲裁）→ 结果故事即正史，路由给 B
   （若与 B 的同期动作冲突，经路由节点静态规则消解，Level 1）
3. 正史下发：
   A 域 → B 的投影演出"被命中" → A 的故事完整
   B 域 → A 的投影演出"出手"，B 本人演出受击 → B 的故事完整
4. 两域各自呈现（允许 100ms 级呈现差），因果结论一致，永不合账
```

### 7.4 巨域问题的消解

2000 人国战 = 每个玩家的订阅列表变长，而非系统合并出怪物：

- 每个玩家的域内：1999 个投影，按 AOI 分级（P0 占绝大多数）。
- 服务器：路由规模 O(N·订阅半径)，配合分级降频线性可控。
- 品类参数示例（六龙经验参数化）：战场内投影心跳 1/6Hz 档、禁 AOE（规则层，见第 8 章）、傻大黑粗表现（低信噪比环境的最小可读单元）。

---

## 8. 规则立法：可交换化设计准则

### 8.1 立法原则

> 一切玩家间交互的规则，必须能在"双方各自为政、时序模糊 ±200ms"的前提下产出唯一且双方均认可的正史。写不出这种规则的设计是非法设计，策划案阶段打回。

### 8.2 可交换化手法清单

| 手法 | 说明 | 示例 |
|---|---|---|
| 双生效 | 双方效果都成立，顺序无关 | 双杀：同时/先后开枪，两人皆死 |
| 偏袒行为者 | 本地看到的命中即命中 | favor the shooter |
| 宽限窗口 | 时序边界模糊化处理 | 前摇内被击穿按部分格挡结算 |
| 事后补偿 | 死亡不作废已出手动作 | 死后弹道继续生效 |
| 资源复制 | 消灭互斥资源 | 个人掉落制，人手一份 |
| 前摇窗口 | 声明与结算分离，收编延迟 | 技能 0.35s 前摇 = 因果对齐窗口 |
| 死亡延迟 | 结果与公开解耦，留排演时间 | 致死判定后 200ms–1s 窗口编排表现 |

### 8.3 需要 Rollback 的残余场景及规则级规避

唯一真正需要回滚的是"玩家基于错误信息做了不可逆决策"。规则级解法 = **信息保守化**：呈现层宁可滞后也不抢先——状态未确认前画面保持原样，把"可能被推翻的体验"消灭在呈现之前。

### 8.4 公平的重定义

公平 ≠ 裁决出客观先后（不存在）。公平 = **规则对模糊性持对称态度**。双杀对双方同等"不利"，故公平；回滚推翻一人体验保全另一人，即使逻辑正确，体验上即不公。

---

## 9. 时空碰撞

### 9.1 问题

实体各有时间线，"同一时刻两个物体在哪"无意义。碰撞从三维几何问题变为**时空几何问题**。

### 9.2 扫掠体模型

- 实体 A 在一次心跳内走出时空线段 `L_A = {(t, p₀+α(p₁−p₀))}`。
- 碰撞 ⇔ 存在时刻 t*，两实体的扫掠体在空间相交。t* 是解出的未知数。
- 对方在"我"的时间轴上的轨迹，用其故事样本插值重建（Hermite，见第 10 章）。

### 9.3 三级检测

| 级 | 内容 | 用途 |
|---|---|---|
| L0 粗筛 | 扫掠 AABB 时空重叠测试（时空索引，键 = x,y,z,t） | 否掉 99% 实体对 |
| L1 轨迹求交 | 扫掠胶囊体解析求 t* | 移动推挤、近战、普通弹道 |
| L2 升频精判 | 触发双方故事升频后重判 | 技能命中、竞技判定 |

### 9.4 升频规则

**交互烈度决定故事频率下限**：进入"扫掠体可能相交"关系即触发共识域协议提升双方故事频率。不是距离触发升频，是碰撞可能性触发升频。

### 9.5 回溯性命中

碰撞检测必然在 t*+δ 完成 → 产生回溯事件 → 由正史裁决器与规则集（第 8 章）裁定 → 修正双方时间线 → 呈现层执行叙事修复。同帧双杀类悖论自然消解：因果顺序由时空几何中的命中时刻决定，而非消息到达序。

---

## 10. 呈现层：把故事演成画面

### 10.1 渲染实体

渲染层 = 一个心跳为刷新率的实体，通过订阅通道收故事。每帧：

```
1. 收故事：排空 inbox，按时间戳归入各可视实体的时间线
2. 重建视图：物化"此刻画面需要什么"（仅供观看的副本，不求真，求自洽）
3. 插值：在每个实体的局部时间线上求值
4. 翻译：语义 → 演出编排 → 资产指令
5. 绘制
```

### 10.2 插值与时间

- **插值 = 用延迟换连续性**：渲染呈现的是 t_r − Δ 时刻的世界，Δ = 采样间隔 + 抖动缓冲。
- **Δ 逐实体分化**：画面是一幅"时间纵深图"而非时间切片（近处实体呈现在 60ms 前，远处 6 秒前，石头呈现在"永恒"）。与物理视觉经验同构，玩家无感。
- **Δ 呼吸调节**：缓冲将耗尽 → 快速扩 Δ（+5ms/帧）；缓冲充裕 → 缓慢缩 Δ（−2ms/帧）。宁多延迟，不要抖动。
- **导数补偿稀疏采样**：故事方言须携带速度（一阶）乃至加速度（二阶）。1/6Hz 实体经 Hermite 样条可补成流畅曲线；弹道类直接解析外推。
- **α 必须用真实时间戳计算**，禁用帧序号推算（心跳不等间隔，帧数推 α 必生 judder）。

### 10.3 叙事修复（乱序/迟到/丢失）

| 情况 | 策略 |
|---|---|
| 迟到故事，偏差小 | 无视（已呈现内容优先） |
| 迟到故事，偏差中 | 300ms 内平滑收敛归位 |
| 迟到故事，偏差大 | 硬切 + 表现掩盖（特效/镜头切换） |
| 故事断档 | 按末态外推续走；超时冻结为雕像 |

总原则：**画面连续性 > 数据准确性；已呈现因果不可推翻 > 呈现及时性。**

### 10.4 呈现侧分层

```
R0 故事接入（品类中立，入底座）：订阅管理、时间线物化、乱序修复
R1 语义翻译（方言绑定，跟品类走）：故事 → 演出时间轴
R2 资产执行（引擎绑定，跟引擎走）：动画状态机、特效、音频、场景图
```

- 动画状态机的驱动源 = 故事片（收到"X 施法"故事 → 切换施法动作），状态机即方言的可视化语法。
- **资产 = 慢故事**：模型/动画/场景网格是心跳极慢、体积巨大的实体故事，加载复用故事通道（首包全量 + 增量）。换装 = 讲一个"外观故事更新"。
- 电影化叙事 = 高优先级故事源临时接管演出编排器，演毕归还。

---

## 11. 导演层（服务器侧智能）

### 11.1 服务器职责（按比重排序）

- **邮局（本体）**：路由故事——通道管理、订阅匹配、分级降频、带宽分配。服务器的绝大部分算力在此；
- **实体宿主**：承载所有实体进程的运行环境，玩家实体的业务结算在此完成（结算即正史，见 6.4）；
- **导演（按需）**：扮演 NPC 投影、编排动态事件、离线补史；
- **规则消解器（少量）**：路由路径上的静态冲突规则，无状态纯函数；
- **临时仲裁（极少数）**：仅 P3 强耦合域，用完即毁。

### 11.2 替身与预演

- 空间索引预测两个玩家将接触 → 提前数秒在各自域中生成对方替身开始排演 → 正式订阅建立时无缝接管。
- 替身的排演质量决定"遇到真人"的无感度，是导演系统核心质量指标。

### 11.3 惰性模拟与不可证伪

- 无观察者区域：实体沉睡或统计级模拟。
- 模拟预算从"诚实"转移到"不可证伪"：离线三天的村庄，只要与玩家记忆不矛盾，历史可补造。
- **玩家记忆优先级高于世界**：玩家记得的事必须保持为真；玩家不知道的事可自由改写。正史（战报/回放/存档）= 固化的记忆，是全世界必须遵守的部分。

### 11.4 离线补史

玩家重新上线时：个人域与关系通道沉淀的正史对齐（"你不在时好友屠了龙"），以不可证伪方式编入其世界线。

---

## 12. 品类参数化（架构验证用例）

架构成立与否，以能否不改 L-1/L0/L1 装配出以下品类为准：

| 品类 | 关键参数 |
|---|---|
| MMO 主城 | 空间通道为主，P1–P2 投影为主，心跳 20Hz |
| 千人国战 | 投影心跳降至 1/6Hz 档、规则层禁 AOE、表现傻大黑粗、Δ 呼吸放宽 |
| SLG 战报 | 全交互经 Level 0 结算并全量留痕，战报 = 正史重放 |
| 换装/社交 | 外观方言（高组合度、可靠必达）、关系通道为主、呈现层预算最大化 |
| 格斗 | P3 同步订阅（临时共识域），逐帧输入级交换 |

---

## 13. 实施路线（指导 AI 构建）

### 里程碑 M0：最小细胞（目标：1000 行内核）

两个实体 + 一条通道 + 一种方言：

- L-1：单尺度网格索引；
- L0：信封路由（内存版）；
- L1：两个实体各有心跳与时间线 ring buffer；
- 演示：A 讲故事 → B 收故事 → B 的时间线重建 A 的轨迹 → 简易插值呈现。
- **验收**：B 端呈现位置与 A 端真实位置的误差分布可测量。

### 里程碑 M1：投影交互闭环

- 投影分级 P0–P2、正史裁决器、一种可交换化规则（双杀）；
- 两个"玩家"各持一域，互相以投影交互；
- **验收**：双方各自故事自洽；无回滚；乱序注入下正史唯一。

### 里程碑 M2：呈现完整化

- R0–R2 分层、Δ 呼吸调节、Hermite 插值、叙事修复策略栈；
- **验收**：1/6Hz 投影在 60Hz 渲染下视觉流畅（人评 + 呈现误差指标）。

### 里程碑 M3：密度压力

- 时空索引（碰撞 L0 粗筛）、订阅 AOI 分级、心跳自适应律；
- 模拟 2000 投影同域；
- **验收**：单域内存与 CPU 随投影数线性；P0 级投影成本 ≈ 0。

### 里程碑 M4：导演系统

- NPC 扮演投影、替身预演、离线补史；
- **验收**：玩家盲测无法区分投影与真人接管瞬间；离线回归后世界线无矛盾。

### 全局验证指标

1. **呈现误差**：渲染位置 vs 事后真实位置的偏差分布（P50/P95/P99）；
2. **Δ 呼吸频率**：每实体每秒 Δ 调整次数（目标 < 0.1）；
3. **正史延迟**：意图 → 正史下发的耗时分布；
4. **回滚率**：每千次交互触发回滚次数（目标 = 0，除信息保守化失败外）；
5. **通道 B 占比**：关系通道流量占比（运营健康度）。

---

## 14. 风险清单

| 风险 | 说明 | 缓解 |
|---|---|---|
| 替身质量 | 替身与真人行为差异致"画风突变" | 预演提前量 + M4 专项验收 |
| 回溯级联 | 碰撞回溯修正传播给第三方 | 规则可交换化立法，回滚率指标归零化 |
| 调试困难 | 无全局时钟，无"世界在某时刻的状态" | 观测旁路：实体自愿输出统一度量事件流 |
| 策划心智 | "全局时间 0.35s"式思维失效 | 工具链把协议翻译回人类语言；立法准则内建进策划案审核 |
| 方言碎片化 | 方言无限增殖致互通困难 | 方言注册表 + 核心方言集收敛（移动/战斗/外观/社交 四种起步） |
| P3 滥用 | 品类图省事全用同步级订阅 | P3 设成本审计，默认禁用于非格斗品类 |

---

## 15. 术语表

- **域（Domain）**：一个玩家的私人世界，他是其中唯一真人。
- **投影（Projection）**：域内除主人外的一切实体呈现，故事源为真人（路由）或服务器（扮演）。
- **故事（Story）**：实体间交换的带信封不透明字节流，系统的唯一流通物。
- **方言（Dialect）**：一种故事的格式约定，端点私约，传输层无感。
- **正史（Canon）**：服务器裁决生成的终局事件记录，各域独立呈现，永不改写。
- **时间线（Timeline）**：实体的状态历史环形缓冲，服务碰撞、插值、留痕。
- **时间纵深图**：渲染画面各实体处于不同过去时刻的现象（Δ 逐实体分化）。
- **三级参与**：订阅 → 同步（P3，临时共识域）→ 仲裁，成本逐级跳升。
- **可交换化**：使交互结果与顺序无关的规则设计手法总称。
- **叙事修复**：呈现层对乱序/迟到/丢失故事的视觉掩盖策略栈。
- **不可证伪**：模拟的唯一约束——不得与玩家记忆矛盾。

---

*本文档由一场关于游戏架构本质的讨论沉淀而成：从三十年心跳内核的技术资产，到密度哲学与付费结构的经验，最终推导至"一人一世界"的投影域架构。架构的每一条公理，都能在工程史中找到其代价与证据。*
