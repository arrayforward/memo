# HESP：世界计算机输入层协议

**High-Entropy Supply Protocol —— 高熵数据流的采集、确权与切片**

版本 1.2 / 2026
本文自包含，无需任何前置阅读。

---

## 1. 6G 需要新的流量

6G 已经把路修好了：Tbps 级峰值带宽、亚毫秒时延、确定性网络切片。但跑在路上的车没有跟上——视频、游戏、消费级 XR，在 5.5G 时代就吃饱了。网络能力与内容需求之间出现了脱节：如果没有新的流量形态出现，6G 有沦为 5G 扩容版的风险，投资回收周期会被显著拉长。

什么样的流量才配得上这张网？至少要满足三条：**不可压缩**（缓存命中后还能持续拉动骨干网）、**不可预生成**（不能由某个节点本地复现，必须经过网络传输）、**实时发生**（延迟即失真，需要确定性切片保障）。现有内容形态一条都不占，而 AI 合成内容让局面更糟：生成内容低熵、可缓存、可被任何节点本地复现，AI 越强大，合成内容越充沛，骨干网越空转。6G 的容量危机不是技术危机，是内容供应链危机。

好消息是，这样的流量源其实遍布世界，只是从没被接进来：

- **人**。一个驾龄十年的司机，在雨夜的陡坡急弯前犹豫了三秒。这三秒花多少钱都合成不出来——不是因为它复杂，而是因为它分布外：真实的人在真实的风险面前真实的迟疑，携带着十年驾龄压缩成的因果痕迹。传统范式里，这三秒要么消失在行车记录仪的循环覆盖里，要么被平台免费捡走、事后标注——而用户没有动机产出分布外的行为，平台捡到的永远只是模型已经会的东西。采集的方向就错了。
- **设备**。家庭摄像头、车载记录仪、工业传感器昼夜观测物理世界，但它们是哑终端：能看，不能说；说出来的，没人确权，没人付费。
- **公共与企业的存量**。政府的公域传感器（交通、气象、河道）、企业的产线记录与调度日志，本来就在运转，本来就在产生——接进来就是财政收入通道与企业收入来源。

这三类源还有一个容易被忽略的共同处境：它们的数据每天都在产生，也每天都在消失。犹豫的三秒被循环覆盖，电流的异常波动被日志滚动冲掉，河道的变化没人记录就只是一段没人知道的过去。不是谁刻意浪费，是缺一个接住它们的地方。

而它们值得被接住的原因，埋在数据本身的产生方式里：这些流是一秒一秒过出来的。一个司机的十年驾龄、一条产线的十年运转，只能被连续经过，不能被快进、不能被跳过、不能事后补拍。这一点此刻看起来只是常识，后文会看到，它是整个协议设计反复回到的那个支点。

流量有了，谁为流量付费？这正是巧的地方：最需要高熵数据的一方，恰好也是付费能力最强的一方——AI。AI 需要高熵信息源，这不是某个模型的偏好，是学习的本质：模型的能力上限由它见过的真实世界的多样性决定，合成数据喂不出分布外的能力。而这个需求的终点不是 AI 自己——**AI 需要高熵信息源，是因为人要 AI 更好地服务人**：司机雨夜的三秒犹豫，最终变成自动驾驶在同类路况下少一次事故；产线两周前的电流异常，最终变成不停机的工厂。数据从人处来，价值回到人处去。于是三方同向：AI 得到训练数据，6G 得到流量，数据源得到报酬——缺的是让这件事可持续的机制：数据源必须被确权、被定价、被持续偿付，否则供给不会发生。

HESP 做的事只有一件：**把这些真实世界的高熵数据流接进 6G 网络——为它们确权、定价、切片，让 6G 有新流量可跑，并在验证合格后把流交给上层的计算系统。** 这就是「输入层协议」的含义。

## 2. 协议边界：流的来路、去处与回声

数据流被接进来之后，并不是到 HESP 为止。它往上还要经过两层，这两层不是本协议的内容，但 HESP 的形状由它们决定，需要先说清楚。

**再往上是内核，一台「把信号变成事实」的机器。** 物理世界被切成若干自治域——一辆车、一个路口、一座港口各是一个域，对自身状态有绝对权威。传感器的高频样本先落进可丢弃的感知日志，经迟滞状态机过滤，只有跨过双阈值并持续驻留的「状态迁移沿」才配进入决策环；决策环内先沙盒预演，通过则原子写入域的 append-only 事件日志——**一次结算即成事实（FACT），永不推翻；错了不删除，追加新事实**，法院不删除误判，而是做出新判决。事实如何结算，是内核的事，HESP 不干预。

**最上是输出层，负责事实被谁看见。** 每个用户（或人的分身、机器人）按需订阅相关实体，已结算的事实经路由送达、成为各自空间里的呈现；无人订阅的实体不耗算力——存在即被感知。事实如何呈现，是输出层的事，HESP 不干预。

```
┌─────────────────────────────────────────────────┐
│ 输出层：订阅 / 呈现 / 具身在场                     │  ← 事实 → 在场
├─────────────────────────────────────────────────┤
│ 内核：自治域 / 决策环 / 一次结算即成事实            │  ← 信号 → 事实
├─────────────────────────────────────────────────┤
│ 输入层：HESP（本协议）采集 / 确权 / 切片            │  ← 真实 → 机器
└─────────────────────────────────────────────────┘
```

三层共用一条脊椎：append-only 的事实档案与同一套网络切片资源。三层分工一句话：**HESP 负责流的来路（谁产的、真不真、值多少），内核负责流的去处（信号如何成为事实），输出层负责流的回声（事实如何被看见）。**

HESP 与邻居层的接口只有两个，都在第 7 章：向上的**感知接口**（验证合格的包交给内核），横向的**切片接口**（与输出层共享同一网络切片资源池）。

## 3. 不变量

协议的全部主张压缩为四条。任何新机制提案、任何争议，先问它与哪条不变量冲突；不冲突的，协议无权干预。

**I. 真实性只锚定在物理层。**
能判定数据流真伪的证据只有三类：出处链完整、物理层痕迹存在（硬件时间戳签名、空间一致性、无线电空口记录）、时间深度可验证。一切统计判别——熵值估计、行为指纹、分布亲缘——是**定价信号**，只影响价格，永远无权拒绝登记或给数据定罪。统计判别随生成模型增强必然失效，所以它不守门；守门的东西必须不随模型能力退化。

**II. 价值主张必须可重跑。**
任何定价与归因主张，必须附带可复现实验包（随机种子、数据哈希、执行环境、结果全登记）；任一方可重跑验证。规则可以被博弈，实验可以被重跑。

**III. 对手能力一次性声明。**
协议假设对手：持有真实数据流、任意算力、可与至多一个内部角色合谋。协议的真实性主张在此对手模型下一次说清，不随攻击花样逐条回应。

**IV. 协议只管证据，不管行为。**
不规定价格，不规定授权如何取得，不规定市场结构，不惩罚任何人。只生产证据：append-only 记录、可复现实验、物理层采样点。隐私、同意、分润公平、反垄断，由法律与市场依据协议生产的证据裁决。法律负责裁决，技术负责举证。

## 4. HES Packet：最小完整单元

HES Packet 是一次可路由、可验证、可切片的高熵数据流的最小单元，一切确权与计价只以包内声明为准：

```
┌──────────────────────────────────────────────────────┐
│ magic / format_version / packet_id (128-bit)          │
│ source_type      : human | device | public | enterprise│
│ topic_embedding / drift_type / required_time_scale    │
│ patch_class      : S | G | E                           │
│ entropy_estimate : bits/s（定价信号，非真伪证据，I）    │
│ realtime_class   : hard | soft | offline               │
│ slice_requirement: S0 | S1 | S2 | S3                   │
│ bandwidth_profile: { peak_bps, mean_bps, duration_s }  │
│ cost_meta        : 代价元数据（按源类型的强制字段集）   │
│ consent_scope    : 授权范围声明（空或越界即无效包）     │
│ provenance_chain : append-only 出处链                  │
│ delta_payload    : 数据流本体                           │
│ label + confidence（归类器输出，可追加 LabelCorrected） │
│ issuer_signature / hes_signature（哈希链＋签名）        │
└──────────────────────────────────────────────────────┘
```

`cost_meta` 公共项：`hardware_ts_sig`、`duration`。按源追加：human 加 `interrupt_count`、`hesitation_rt_dist`、`exit_possible`；device 加 `device_attestation`、`uptime_profile`；public 加 `operator_attestation`、`asset_registry_ref`；enterprise 加 `entity_attestation`、`asset_registry_ref`、`data_profile`。缺所属 profile 任一字段的包视为无效包。

包进入内核后的命运见 7.1：HESP 保证包的来路与真实性，是否成为事实由内核判定。**HESP 的「包」与内核的「事实」之间的全部转换，就是输入层与内核的边界。**

## 5. 需求登记、采集与供给生命周期

**需求侧：高熵信息源是刚需，不是偏好。** 模型的能力上限由它见过的真实世界的多样性决定；要让 AI 在真实场景里更好地服务人——开车、巡检、陪护、调度——它就必须见过真实场景，包括那些罕见、尴尬、分布外的场景。需求不会枯竭：新行为制造新的未知，现实本身在流变——对高熵数据的需求不是存量，是流量。

**需求的操作机制：盲区地图。** 「更好地服务人」是抽象目标，盲区地图是把它变成具体数据需求的转换器，两条路径：

- **服务盲区 → 服务质量的直接提升**。AI 在服务中力有不逮之处，就是盲区：自动驾驶拿不准的路况、陪护机器人读不懂的犹豫、调度模型没见过的作业模式。模型方维护对自身不确定性的持续登记（盲区地图，模型方私有，协议不设全局注册表），据此向数据源签发请求——「山城夜间雨雾中的窄路会车」「在 XR 环境中连续使用一个月，记录全部不适与即兴决策」。**补一个盲区，就是补一处服务能力的缺口**——盲区地图因此同时是 AI 服务人类的施工图。
- **现实漂移 → 世界模型的持续更新**。树被砍、店换招牌、产线工艺漂移——现实在流变，模型不更新即腐烂。变化巡检持续扫描变化信号，命中即立项采集。**盲区地图因此同时是世界模型的保鲜清单**（与上层镜像的腐烂坐标同源，见 7.2）。

盲区地图有个逻辑极限：它只能覆盖「已知的未知」，模型无法表征的区域系统性缺席。所以供给天然双层：被请求的供给由盲区地图引导，未被请求的供给由真实世界的探索行为产生——**地图把盲区变成问题，而边疆由真实世界本身定义。**两条供给路径在协议内同权：确权与计价对两者一视同仁，市场按实际贡献给两者定价。

**带宽分级（切片即定价变量）**。补丁的熵值与实时性映射切片等级——切片不是传输选项，是定价变量：

| 级别 | 定义 | 切片 | 典型场景 |
|---|---|---|---|
| Patch-S | 单帧观察＋时间戳，保鲜级 | S0/S1 | 海量低峰连接，给 6G 打底 |
| Patch-G | 策略对抗行为流，博弈级 | S2 | 玩家、红队钻出的未覆盖路径 |
| Patch-E | 长期全息委托，前沿级 | S3 | 一座自动化港口的 Gbps 级十年委托流 |

**供给状态机（含退出即信号）**：

```
IDLE → LOCKED → ENGAGED ──→ SETTLED
           │        │
        E1 离线  E2 退出 / E3 挑战失败
           ▼        ▼
        Exit_Answer → 按已有轨迹正常结算
        E3 → ADVERSARIAL（转欺骗策略货架计价，不拒绝、不删除）
```

ENGAGED 内部叠加连续量 `engagement_gradient`（中断率、带宽衰减、犹豫漂移的日度聚合斜率）：梯度下行即渐退，协议自动逐级降切片，渐退期流照常计价——「源是怎样慢慢退出的」比「源退出了」信息量更大，退出前奏标注为一等高熵信号。退出理由哈希封存，登记即解锁，按已有轨迹正常结算。

**归类器**：输出 `(label, confidence)`：`genuine` / `performed` / `adversarial` / `unknown`（转探索货架，低价入库不丢弃）。标签概率化、可追加 `LabelCorrected`（历史不改写，差价补付）。归类器代码与基准数据集公开；归类器只影响价格（I），无权拒绝包。

## 6. 硬编码

```
assert records.append_only == true        // 记录即事实：不删除，只追加
assert consent.required == true           // consent_scope 空或采集越界即无效包
assert consent.revocable == true          // Consent_Revoked 即时生效；历史不删，已结算不追回
assert statistical_signals.pricing_only == true   // 统计判别只是定价信号（I）
assert synthetic_content.slice_premium == 0       // 合成内容不享切片溢价
assert classifier.code_and_gold_set_public == true  // 归类器代码与基准集公开
```

隐私出端形态、被观察者权益、费用分摊、市场结构——均为实现层与法域事务，协议只提供可审计接口，不进硬编码。

## 7. 层间接口

### 7.1 感知接口（HESP → 内核）

验证合格的 HES Packet 投递到目标域后，走内核的标准入口：

1. `delta_payload` 写入该域**感知日志**（滚动缓冲，非真源，可丢弃）；
2. 过迟滞状态机与聚合网关——只有跨过迁移沿的才成为候选；
3. 进决策环：沙盒预演，通过则结算为 FACT，写入域事件日志；
4. HESP 侧的 `packet_id` 与内核侧 FACT 的 `eventId` 互记因果指针——**确权链（HESP）与事实链（内核）在此缝合**：定价沿确权链追溯到包，定责沿事实链追溯到事件，两条链各自单调可读。

HESP 不承诺包一定成为事实——是否跨过迁移沿由内核依环境判定，正如内核不承诺包的真实性——来路由 HESP 判定。**输入层保证「这是真的」，内核决定「这算不算数」。**

### 7.2 孪生保鲜接口（HESP ↔ 内核）

内核与输出层靠惰性维持算力可行：无人订阅的实体与区域沉睡，不耗算力。但沉睡的现实在流变——树被砍、店换招牌、河道改道，镜像不维护即腐烂，腐烂即与观察者的感知矛盾（穿帮）。机器普查是稀疏的，HESP 的分布式源是连续驻场的：

- `topic_embedding` 与镜像区域同嵌入空间，Patch-S 流可直接唤醒对应沉睡区域——盲区地图与镜像腐烂处是同一批坐标；
- 唤醒后生成的状态须不与已结算的感知事实矛盾，HESP 流构成这些约束的一部分；
- 一份保鲜流可同时供给模型方（训练）与内核（镜像维护），两次计价互不冲突——**上层的核心成本项，就是输入层的核心收入项。**

### 7.3 切片接口（HESP ↔ 网络，与输出层共享）

输入层的 S0–S3 与输出层的订阅分级共享同一网络切片资源池：上行高熵流按熵值与实时性计费，下行状态流按体验颗粒度计费。**带宽第一次同时向两个方向变现**，中间是同一套切片资源。S0–S3 为协议内逻辑命名，与任何外部标准标识的映射由接入网适配层完成——标准组织改了编号，本协议一个标点不用动。

### 7.4 事实档案接口（三层共用）

HESP 的权属与流水记录、内核的域事件日志、输出层的交互事实，写入同一种 append-only 归档：哈希链＋签名＋因果指针，最终一致复制，只存哈希不存载荷。全系统不存在跨层强一致部件；终局性在源处成立的一刻已确定，归档只是公证。

## 8. 信任假设（显式声明）

协议不假装「去信任」，只标注信任边界：

0. **时间的连续性**：任何有物理承载的数据流，其时间维度连续、不可逆、不可压缩——十年只能被连续经过。这是全部真实性主张的支点，无法在协议内证明，只能显式声明。
1. **设备可信根**：硬件时间戳签名的信任根是安全芯片厂商密钥；缓解为多厂商设备多样性＋网络侧交叉比对。
2. **基准集治理**：归类器基准数据集的托管与抗腐化是协议外工程；缓解为多源冗余＋交叉互验。
3. **运营商侧可信根**：网络侧校验的信任根是运营商自身的空口记录；运营商与采集商合谋时，协议内无独立第三方物理测量通道。
4. **模型方绕过**：协议只能改变绕开协议自行获取数据的相对成本，不能消灭绕过。
5. **统计判别力衰减**：全部统计信号随生成模型增强而衰减——正因如此它们只是定价信号（I）。这不是残余风险，是设计的出发点。

## 9. 里程碑

| 阶段 | 验收 |
|---|---|
| M0 最小闭环 | 盲区不确定度随轮次下降；注入已知价值补丁，价值排序与真值 Spearman ρ ≥ 0.6 |
| M1 真实接入 | 真人数据源＋真实小模型，价值排序与人工评估 ρ ≥ 0.6（预注册规程） |
| M2 内核对接 | HES Packet → 感知日志 → 决策环全链路打通；packet_id ↔ eventId 因果指针可双向审计 |
| M3 市场成形 | 多源供给、多方采购，价格信号有效；供给方获得持续偿付 |
| M4 长期委托 | 年级全息流接入；退出事件照常计价；孪生保鲜唤醒链路打通 |
| M5 现网对接 | 单源 Gbps 级全息流 72 小时不间断传输＋S3 切片计费全链路 |

**零点启动**（冷启动构件，全部 append-only 登记、版本化演进）：G0 初始盲区地图由 ≥3 个相互独立的开源模型委员会贡献；G1 初始基准集由 ≥3 个独立学术数据集锚定；G2 双轨演进；G3 信任收敛——最初的可信来自公共学术共识，最终的可信由经校验的长期真实流接管。**启动构件是脚手架，不是地基。**

---

**一句话定位**：HESP 为 6G 扩展流量——把真实世界的高熵数据流接进来，可验证、可定价、可审计：AI 得到训练数据，6G 得到流量，数据源得到报酬。数据从人处来，价值回到人处去：人供给真实，AI 服务人类，协议让这件事可持续。管道铺在这里，水会来。
