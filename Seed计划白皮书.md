# Seed 计划白皮书

**让数据在没有任何当代软件的世界里，自己解释自己。**

版本 0.2 / 草案 / 2026

---

## 摘要

Seed 计划定义一种**自描述归档容器格式**及其配套工具链。一个 Seed 档案在被打开时，不假设读者拥有任何当代操作系统、指令集、编解码软件或文档——它只假设读者能读字节和 ASCII 英文（或者，是一个能读英文的大语言模型）。档案开头的一小段明文，教会读者执行一台四规则的通用图重写机；这台机器再执行档案内携带的编解码程序，从而解锁全部内容。档案同时内嵌测试对，使任何读者都能**机械地验证**自己的解码是否正确，而不必信任任何外部权威。

我们把"格式会过时"这个保存领域的根本难题，转化为一次性的、不足 2.5KB 的教学成本。数据不需要随格式迁移而反复转码；语义的钥匙与数据封存在同一个文件里。

---

## 1. 问题：我们建造在流沙上的数字文明

当代信息的载体呈现出一种结构性脆弱：

- **介质层**：磁盘与磁带寿命 5–10 年，需恒温恒湿与持续维护；
- **格式层**：编码格式平均 10–20 年过时。今天的 JPEG、H.264、docx，都依赖特定的、复杂的、终将消失的软件栈才能被读取；
- **生态层**：x86、ARM 这样的指令集，Linux、Windows 这样的 ABI，都是历史的偶然产物。它们消亡后，为它们编译的程序全部成为死字节。

现行的数字保存策略是**永续迁移**：每隔若干年，把全量数据转码到新格式。对 PB 级机构而言，每次迁移意味着数月的算力、人工校验与不可逆的损坏风险，而且这个成本**每十年重新支付一次，直到永远**。迁移链条上任何一环中断——一场战争、一次预算削减、一个机构的解散——数据就在"比特还在"的意义上活着，在"无人能读"的意义上死去。

更根本的问题是：**我们今天写入的一切，都默认了读者与我们是同时代的人。**

## 2. 前提：三样赌注

Seed 档案的生存只依赖三条假设，按脆弱程度排序：

1. **读者能读字节**——任何计算文明都满足；
2. **ASCII 英文可读**——人类存续则自然语言存续；大语言模型存续则英文技术文本必然是其语料的一部分。这是所有可选通信协议中历史表现最好的一个（ASCII 已存活 60 余年，UTF-8 是它的超集）；
3. **存在能"读规格、写程序、跑测试"的执行者**——人类工程师或大语言模型均可，不挑。

凡不在此三条内的知识——任何指令集、任何文件格式、任何软件——都**不允许出现在档案的关键路径上**。我们特别区分两类知识：

| | 自我再生的知识 | 只会衰减的知识 |
|---|---|---|
| 例子 | 算术、组合子逻辑、通用计算 | x86 指令集、JPEG 算法、PNG 格式 |
| 被遗忘后 | 任何计算文明都会重新发明 | 永远不会被重新发明 |
| 档案策略 | 只需"指名唤醒" | 必须完整携带（作为程序或规格） |

档案的正文只能用不会死的语言书写；会死的知识，要么被教会，要么只当附赠。

## 3. 核心理念

### 3.1 存档即教学

一份 Seed 档案本质上是写给未来的一封**可执行教材**。它最反直觉的性质是：教学成本占大头且不可压缩——在一个只存一幅 32×32 圆盘的演示档案里，教学文本占 83%。但这笔成本**只付一次**，之后任意大的负载（图像库、音频、文档、整个软件语料）都沿用同一套已教会的语义，边际成本趋近于零。

### 3.2 自然语言只出现在第一层

自然语言无法机械验证——它可能误读。因此 Seed 的设计纪律是：**任何能从自然语言降格为程序的知识，必须降格**。明文头（≤2.5KB）只负责教会一台四规则的机器；此后的一切——编解码器、格式规格、索引——全部是形式化的程序与数据。误读风险被压缩在最小范围，且由内嵌测试对兜底。

### 3.3 验证即信任

未来读者不该信任我们。档案内嵌三层测试对：

- **语言层**：三个图项必须归约到 41、7、5（组合子语义被无损重建的证明）；
- **指令层**：26+ 条测试向量，锁定虚拟机每条指令的正常路径与全部错误路径；
- **端到端**：每段媒体负载附"解码结果的已知前缀与哈希"。

读者（人或 AI）实现解码器后跑测试对——全对，说明它对了；有错，错在读者而非档案。**批改权随档案封存，不依赖任何活着的权威机构。** 这也是对 AI 读者"幻觉"问题的根本回答：档案不要求 AI 自信，只要求 AI 通过考试。

### 3.4 正确性是档案的本职，效率是读者的本职

档案内的编解码器以简洁、无歧义为先，不追求执行速度。未来读者——尤其是大语言模型——在正确解码一次之后，有无穷的办法加速：JIT、重写、用它的时代的原生工具重建。档案只需保证"第一次一定对"。

## 4. 文明的断代：结绳记事的教训

### 4.1 奇普：一把丢失的钥匙如何抹掉一个文明

16 世纪的安第斯，印加帝国用**奇普（Quipu）**——打了结的绳串——记录人口、税赋、仓储乃至叙事。西班牙殖民者销毁了绝大多数奇普，处死了读绳的专职记录者（quipucamayoc）。绳结留了下来，读法死了。

此后四百年，学界的主流判断是：印加没有文字，奇普只是"辅助记忆的珠子"。直到近二十年，研究者才逐渐证实奇普至少有数字的十进制记法，且很可能编码了远超数字的信息——**一个文明的记录系统一直躺在博物馆里，只是被误判为"原始人的记事绳"**。而读懂它的全部代价，本来不过是一本"结绳规则手册"——殖民时代甚至有西班牙文书官记录过部分读法，那些手册没有被当作需要随绳保存的东西。

玛雅象形文字是同一教训的另一半：碑文遍布丛林，读法随抄写阶层消亡而失传，被当作"神秘图画"三百年，直到 20 世纪中叶才被重新破译。破译之所以可能，靠的是两样幸存物：一把"钥匙"（传教士兰达记录的不完全字母表）和大量**已知答案的样本**（历法数字）——研究者先验证历法读对了，才敢信自己读懂了别的。

这两个案例给出了数字保存的判词：

> **文明的断代，往往不是数据没了，而是"数据如何被读"没了。没有钥匙的数据，会被后代误认为没有数据。**

而玛雅案例同时给出了救赎的配方：**钥匙 + 已知答案的样本 = 重新破译的可能**。这正是 Seed 的"明文头 + 测试对"结构在四百年前的原型——兰达的字母表就是明文头，历法数字就是测试对。我们不是在发明新东西，是在把那次侥幸成功的破译，变成每一次封存时的必然。

### 4.2 时间分层：从一万年前回到今天

保存方案必须与"假设多少东西还活着"匹配。Seed 计划按时间尺度分四层，每层只依赖比它更长的那一层假设：

| 时间尺度 | 假设还活着的 | 钥匙的形式 | 正文的形式 |
|---|---|---|---|
| **一万年+** | 只有视觉与直觉 | **图形规则卡**：物理雕刻的三个符号（● 擦除 / ▲ 复制 / ◉ 组合）与三条图示重写规则，用连续三帧图解一次完整的归约——任何有眼睛的读者都能"看图学会"图计算 | 石刻/陶瓷上的图项与图像，语义即图示 |
| **一千年** | 数学与某种语言 | 自然语言写就的规格文档（数学记号 + 多种语言互译冗余） | 图项 + 源码级描述 |
| **一百年** | LLM 或工程师、ASCII 英文 | **522 字节 ASCII 明文头**：教会四规则图重写机 | SKI 图项 + SeedVM 字节码 + 负载（本白皮书主体方案） |
| **今天** | x86、现有软件生态 | 不需要钥匙 | i386 机器码附赠轨、普通文件原样封装 |

注意这个表的两个性质：

1. **嵌套兜底**：一百年层失败（LLM 不存在了），退化到一千年层（自然语言规格还在）；一千年层失败，退化到一万年层（图形卡还在）。每一层都是上一层的保险，而不是替代。完整的 Seed 存档是**同一份语义的四重誊写**，誊写的语言一层比一层原始。
2. **钥匙随数据衰减**：时间越长，钥匙必须越"不依赖文化"——从英文退到数学，从数学退到图形直觉。而正文始终可以是同一套图重写语义：**图计算是唯一在全部四个时间尺度上都能充当正文的形式**，这正是一万年前先设计图形规则卡、一百年后仍在档案第一层教图重写的深层原因——它们不是两个方案，是同一根语义主轴在时间轴上的投影。

奇普的悲剧是钥匙与绳分离保存；Seed 的全部设计，归结起来就是一句话：**把钥匙刻在数据自己身上，并且用四种耐久的语言各刻一遍。**

## 5. 架构

### 5.1 总览

```
┌─────────────────────────────────────────────────┐
│ 第1层  ASCII 明文头(≤2.5KB)                      │
│         教会:SKI 图重写机 + 容器格式 + 测试对惯例    │  唯一一次教学成本
├─────────────────────────────────────────────────┤
│ 第2层  程序节区                                   │
│         · SeedVM 解释器(SKI 图项,~500B)          │  权威语义
│         · 编解码器(SeedVM 字节码)                 │  格式能力
│         · 测试向量(26+ 条)                       │  信任锚
├─────────────────────────────────────────────────┤
│ 第3层  数据节区                                   │
│         · A级:原生编码的核心内容                  │
│         · B级:原样封装 + 规格随葬                 │  99% 的体积
│         · C级:冷数据,仅登记                      │
├─────────────────────────────────────────────────┤
│ MANIFEST(ASCII 清单)                            │
└─────────────────────────────────────────────────┘
```

### 5.2 第一层：SKI 图重写机

语义底座是组合子逻辑（Schönfinkel 1924 / Curry），我们采用其四原子变体：S、K、I 组合子加整数运算。完整语义只有七条重写规则，一个够格的读者用约 60 行代码即可重建求值器。选择它的原因不是效率，而是**可教学性**：它是人类已知的最小通用计算形式之一，且属于"自我再生的知识"——即使被彻底遗忘，任何研究计算的文明都会重新发明等价物。

### 5.3 第二层：SeedVM——为"被解释"而生的虚拟机

如果每种编解码器都直接写成 SKI 图项，体积将膨胀 10–100 倍且无法维护。因此我们设计 SeedVM：一台定长指令寄存器虚拟机（16×32 位寄存器、22 条指令、每条指令固定 8 字节、无标志寄存器、无未定义行为、全机器仅 6 种错误码）。它的解释器用 SKI 图项写就（约 300–500 字节），随档封存——**这份程序本身就是 SeedVM 的权威语义，形式化、可执行、可验证，不依赖一行自然语言。**

设计取向刻意反主流：定长指令浪费 2–3 倍空间，寄存器机不如栈机紧凑——我们全部接受。因为 SeedVM 的优化目标不是省字节，而是**不可误读**：每条指令的语义互不依赖，可乱序教学、乱序验证；任何两个独立重建的解释器应当在全部 26 条测试向量上逐字节一致。

今天，存量解码器（RLE、DPCM，乃至 zlib 的 inflate）由编译器从 C 子集自动编译为 SeedVM 字节码；一百年后，读者重建 SKI 求值器、执行解释器、运行字节码——三层解释叠加很慢，但第一次解码一定是**对的**。

### 5.4 第三层：数据分级

| 级 | 内容 | 处理 | 体积占比 |
|---|---|---|---|
| A | 文明重启核心：教科书、技术图纸、格式规格文档 | 全量转码为档案原生格式 | 小 |
| B | 重要存量：影像、科研数据、文献 | **原样封装**（字节不动）+ 格式规格随葬 + 采样测试对 | 大 |
| C | 海量冷数据 | 原样封装，仅登记清单 | 最大 |

分级的经济学：格式的复杂度只付一次（每格式随葬一份规格，50KB–1MB），数据的规模一分钱不付（PB 级数据零转码）。对存量文件的打包是 O（文件大小）的纯拷贝操作，吞吐接近磁盘读速。解码 B 级数据的负担转移给未来——因为未来的算力便宜，今天的转码算力贵；我们只需保证**可解性**（规格 + 测试对），不必预付"已解"的成本。

### 5.5 附赠轨：给当代人的便签

档案允许附加当代可直接使用的产物（如 freestanding i386 机器码解码器），但永远标注 `IGNORE if you do not know i386; the term-language version is authoritative`。它是给今天读者的便利，从不在未来读者的关键路径上。古书可以夹一张给当代人的便签，但正文必须用不会死的语言写。

## 6. 信任模型：为什么这套系统值得相信

1. **数学底座**：SKI 语义是数学对象，不随机构、公司、文明兴衰而改变；
2. **冗余互证**：每个解码能力有"自然语言摘要 + 形式化程序 + 测试向量"三种独立陈述，任何单一损坏都会被另外两者暴露；
3. **自校验闭环**：CRC32 护字节，测试对护语义，端到端哈希护意图；
4. **去权威化**：验证不需要联网、不需要 CA、不需要任何活着的参与者——这恰恰是它与区块链式存证的根本区别。

## 7. 应用场景与采纳经济学

### 6.1 今天就有价值的理由

纯"为了未来"的格式无法推广。Seed 档案在今天即提供：

- **当下可用的容器**：原样封装区一键还原为普通文件，用现有软件直接打开；原生编码区配 WASM 查看器，双击即读。用户的数据从不被"锁进"陌生格式；
- **零迁移保险**：替代"每十年一次"的全量转码项目，一次性打包开销典型 <1%；
- **数据溯源凭证**：自描述 + 内置 CRC + 自带解码器的容器，天然满足 AI 训练语料、科研数据的来源与完整性证明需求——这是当下即可变现的特性。

### 6.2 目标客户

不面向消费者。面向**持有法定或使命性长期保存义务的机构**：国家与大学图书馆、档案馆（可嵌入 WARC/BagIt 工作流而非另起炉灶）、科研数据仓（天文、基因组、粒子物理——50 年后仍需复现论文）、合规存档（金融、医疗、航空，法规年限 ≥ 格式寿命）。

### 6.3 成本对比

| | 现状（永续迁移） | Seed |
|---|---|---|
| 每 10 年 | 全量转码：数月算力 + 人工 + 损坏风险 | 零 |
| 一次性成本 | — | 打包 <1% 体积开销 + A 级选择性转码 |
| 失败模式 | 迁移链断裂 → 静默死亡 | 规格、程序、测试对与数据同生死 |

## 8. 路线图

- **M1–M2**（语义层）：SKI 求值器参考实现；SeedVM 解释器与 26 条指令测试向量 —— 已打通原理验证；
- **M3–M4**（容器层）：容器读写、CRC、MANIFEST；SeedVM 的 SKI 权威解释器，双层端到端验证；
- **M5**（工具链）：pack/verify 命令行工具；演示档案（图像 + 音频 + 文档）；
- **M6**（编解码生态）：C 子集 → SeedVM 编译器；inflate（固定哈夫曼子集）移植，接入真实 PNG/zlib 存量；
- **M7**（灯塔合作）：与 1–2 家保存机构共建试点，用"模拟百年后解码全过程"的端到端演示验证叙事。

## 9. 局限与诚实的风险清单

1. **协调风险**：格式价值与使用人数正相关，冷启动依赖灯塔机构；历史上唯一的解法是声望机构先押注；
2. **前提 2 的脆弱性**：若英文与 ASCII 双双消亡，第 1 层失效——对此的终极兜底是把语义**物理化**（图形规则卡、雕刻介质），即退回"一万年场景"的姊妹方案；
3. **AI 读者的能力边界**：我们假设未来 LLM 能读规格、写 60 行求值器、跑测试。当前模型已可做到，但该假设随模型形态演变需要持续检验；
4. **不解决介质问题**：Seed 是格式方案，不是存储介质方案；它假设比特层面有人/有机（如普通的多副本冗余）维持存活；
5. **知识产权**：随葬的规格文档与解码程序需妥善处置许可。

## 10. 与现有方案的关系

| 方案 | 哲学 | 差异 |
|---|---|---|
| 永续迁移（主流） | 跟着时代跑 | Seed：一次性付清，不再跑 |
| PDF/A、WARC 等容器 | 规范当代格式 | Seed 可嵌入其工作流，提供"执行能力"这一它们没有的层 |
| LOCKSS/冗余备份 | 多副本保命 | 解决介质层，与 Seed 互补（Seed 解决语义层） |
| 区块链存证 | 信任活着的网络 | Seed：信任封存的测试对，无需任何活着的参与者 |
| 陶瓷/蚀刻介质（如 GitHub Arctic Vault） | 介质长寿 | 介质上仍写着会死的格式；Seed 回答"刻什么" |

## 11. 结语

数字保存的深层困境，从来不是"比特能存多久"，而是"**意义能活多久**"。我们习惯把意义寄存在同时代的软件里，于是每十年为续命奔波一次。

Seed 计划的赌注可以写成一句话：**把意义直接编码进数据自身，用不会死的语言（数学与英文）写正文，用会死的语言（x86、JPEG）写便签，并把批改权交给档案自己。**

奇普的绳结在博物馆里沉默了四百年，只因为钥匙刻在了别处。我们要让下一个一万年里的每一捆"绳结"，都自带它的读法——从石刻上的图形规则卡，到 522 字节的明文头，四种耐久的语言，同一句话。

一万年后打开它的，无论是人还是别的什么，看到的不是一堆需要考古的字节，而是一位坐在数据旁边的、永不退休的教师。

---

*附：技术细节见《Seed Archive v2 设计说明书》(0.2.0-draft)，两者冲突时以设计说明书与测试向量为准。*
