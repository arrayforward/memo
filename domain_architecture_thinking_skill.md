# 域架构思维技能 (Domain-Architecture Thinking)

> **版本**: v1.0
> **定位**: 面向复杂系统设计的元方法论技能
> **适用**: 分布式系统架构、协议设计、数据基础设施、长期保存系统
> **核心主张**: 在承认物理极限与认知边界的前提下，用显式规则与经济学约束替代隐式假设与全局权威，构建可审计、可退化、可长期存活的系统。

---

## 1. 核心公理（不可妥协）

执行本技能时，以下七条公理具有最高优先级，任何设计决策不得与之冲突。

### 1.1 无全局假设 (No Global Anything)
- **规则**: 主动删除一切无法在物理世界实现的假设
- **检查**: 
  - [ ] 系统中是否存在"全局状态"？→ 删除，改为自治域+索引+订阅
  - [ ] 系统中是否存在"全局时钟"？→ 删除，改为本地逻辑时钟+因果偏序
  - [ ] 系统中是否存在"全局全序"？→ 删除，改为域内全序+局部权威定序
  - [ ] 系统中是否存在"全局共识"？→ 删除，改为最终一致+事实终局
- **口诀**: "凡需全局视图的，都是设计缺陷"

### 1.2 事实终局 (Append-Only Reality)
- **规则**: 历史不可变，现在可更新；作废的是"值"，不是"历史"
- **检查**:
  - [ ] 是否使用 CRUD 中的 U/D？→ 改为追加事件（`LabelCorrected`, `Consent_Revoked`）
  - [ ] 审计是否为单调读？→ 事件日志即事实记录
  - [ ] 下游视图是否通过投影自然得到修正后终态？
- **口诀**: "不删除、只追加；不推翻、只演化"

### 1.3 存在即被感知 (Lazy Evaluation)
- **规则**: 无观察者/订阅者的实体不模拟，算力成本与"被观察表面积"成正比
- **检查**:
  - [ ] 是否有零订阅者实体仍在消耗算力？→ 冻结/沉睡
  - [ ] 唤醒时是否只需满足"不可证伪约束"（不与邻近 P2/P3 订阅者矛盾）？
  - [ ] 算力成本是否随"世界体积"而非"观察表面积"增长？→ 重构
- **口诀**: "没人看的戏不拍"

### 1.4 基础设施中立 (End-to-End Semantics)
- **规则**: 路由/网络/档案层不理解、不模拟、不仲裁内容
- **检查**:
  - [ ] 基础设施是否打开过事件信封的 payload？→ 必须是不透明字节
  - [ ] 隐私是否依赖"承诺不收集"？→ 升级为"架构上无法被未授权者看到"
  - [ ] 归档层是否存储了可解读载荷？→ 只存哈希+签名+因果指针
- **口诀**: "邮局不读信"

### 1.5 确定性纯函数流水线 (Deterministic Pipeline)
- **规则**: 冲突消解前移到设计时消除，而非运行时仲裁
- **检查**:
  - [ ] 决策流程是否为确定性纯函数？→ 输入固定则输出逐比特固定
  - [ ] 是否区分"决策级反事实"（可复现，定责依据）与"物理级反事实"（概率分布，工程参考）？
  - [ ] 软逻辑是否为三步流水线：规则过滤→价值argmax→意图承诺窗口？
- **口诀**: "冲突在数学上不可能发生"

### 1.6 成本驱动与伪造经济学 (Cost-Driven Design)
- **规则**: 用经济学约束替代绝对安全承诺
- **检查**:
  - [ ] 置信度阈值是否由成本比推导：`θ = C_fn / (C_fp + C_fn)`？
  - [ ] 防作弊目标是否为"伪造期望收益 E ≤ 0"，而非"不可能伪造"？
  - [ ] 是否绘制了"伪造成本曲线"与"诚实成本曲线"，确认两线必相交？
- **口诀**: "让作弊不划算，而非不可能"

### 1.7 时间分层与嵌套兜底 (Temporal Degradation)
- **规则**: 不同时间尺度匹配不同假设强度，形成退化链
- **检查**:
  - [ ] 是否显式回答了"当这个假设失效时，系统退化成什么"？
  - [ ] 是否提供了多层保险（如奇普的四重誊写）？
  - [ ] 每一层是否是上一层的保险而非替代？
- **口诀**: "设计退化路径，而非假设永存"

---

## 2. 思考技能 (Thinking Skills)

### 2.1 物理第一性推理
**触发**: 当设计涉及时间、空间、算力、通信时
**执行**:
1. 列出当前设计依赖的物理假设（光速无限？全局同时？算力无限？）
2. 逐一标记哪些假设在物理上不可实现
3. 用更弱的、物理可实现的原语替换（如用因果偏序替换全局时钟）
**示例**: Handoff 状态机处理"权威归属在移动"的窗口期歧义

### 2.2 哲学命题工程化
**触发**: 当遇到抽象概念（真相、存在、感知、事实）时
**执行**:
1. 将哲学命题转化为工程约束（如"存在即被感知"→惰性计算）
2. 定义可验证的工程指标（如沉睡比、唤醒延迟）
3. 排除形而上学不可判定的部分
**示例**: "事实终局"转化为 append-only 事件日志与单调读审计

### 2.3 边界显式化
**触发**: 当设计涉及信任、安全、隐私、合规时
**执行**:
1. 显式列出"本系统不解决什么"
2. 显式列出"信任假设清单"（残余信任假设）
3. 显式列出"设计取舍"（策略A vs 策略B，为何选A）
4. 将边界声明写入协议/架构文档的固定章节
**示例**: 附录B残余信任假设声明；Handoff策略A与策略B的显式取舍

### 2.4 反事实分层
**触发**: 当需要审计、定责、复盘时
**执行**:
1. 区分"决策级反事实"（给定信息集+规则版本，决策是否合法？）→ 逐比特可复现
2. 区分"物理级反事实"（若提前0.5s制动能否避免碰撞？）→ 只出概率分布
3. 前者进审计报告，后者只进工程迭代参考
**示例**: AGV追尾定责：复现软逻辑流水线，而非物理仿真

### 2.5 状态机显式化
**触发**: 当涉及切换、迁移、边界跨越时
**执行**:
1. 将隐式切换转化为显式状态（TRACKING→PRE_HANDOVER→COMMIT→TRACKING）
2. 定义唯一的承诺点（COMMIT点），此前可回退，此后不可逆
3. 定义窗口期的补偿规则（在途争议就地终局、乒乓抑制）
**示例**: 移动Handoff状态机；HES状态机（IDLE→LOCKED→ENGAGED）

### 2.6 教学即设计
**触发**: 当设计需要长期存活、跨代际传承时
**执行**:
1. 假设读者是未来的AI/人类，不共享当代知识
2. 将"解读方法"编码进数据本身（自描述）
3. 提供已知答案的测试对（钥匙+样本=破译可能）
4. 使用最不可能死亡的语言（数学、图形、ASCII英文）
**示例**: 奇普档案的ASCII头教会四规则图重写机

### 2.7 经济化约
**触发**: 当遇到"如何调参""如何防作弊""如何定价"时
**执行**:
1. 将技术争议转化为成本争议（"误刹车一次亏多少钱"）
2. 绘制成本曲线（伪造成本 vs 诚实成本，确认相交）
3. 用稀缺系数、期权费、占用费等市场机制替代固定定价
**示例**: 阈值由成本比推导；HESP的切片期权机制

### 2.8 迟滞无处不在
**触发**: 当系统可能出现振荡、抖动、乒乓切换时
**执行**:
1. 引入双阈值（进入阈值 < 离开阈值）
2. 引入时间迟滞（持续N秒才触发）
3. 统一迟滞机制（订阅建撤、精度升级、安全区、状态切换共用同一套）
**示例**: 施密特触发器；订阅P0-P3的迟滞建撤；Handoff边界重叠带

### 2.9 三层过滤思维
**触发**: 当处理高频信号、噪声、传感器数据时
**执行**:
1. 第一层：滚动缓冲（感知日志，非真源，可丢弃）
2. 第二层：迟滞状态机（双阈值+驻留时间，边界抖动不可能通过）
3. 第三层：聚合网关（窗口内同源同类合并，下游消费定性事实）
4. 最终输出：迁移沿候选（进入决策环）
**口诀**: "事件频率挂钩环境真实变化率，与传感器采样率脱钩"
**示例**: 雨雾天激光雷达：3000样本/秒→0事件→1条FACT

---

## 3. 执行流程 (Operational Playbook)

### 3.1 设计审查流程
当接到一个系统设计任务时，按以下顺序执行：

```
Step 1: 全局假设清除
  └─ 列出所有"全局"字眼（全局状态/时钟/全序/共识）
  └─ 逐一替换为局部原语
  └─ 输出: 域划分图 + 权威归属表

Step 2: 事件溯源转换
  └─ 将所有状态变更转化为事件流
  └─ 确认 append-only 约束
  └─ 输出: 事件类型清单 + 事实终局声明

Step 3: 惰性计算审计
  └─ 标记所有实体的订阅级别（P0-P3）
  └─ 确认零订阅者实体已沉睡
  └─ 输出: 订阅拓扑图 + 算力成本预估

Step 4: 中立性检查
  └─ 画出基础设施层，确认不打开 payload
  └─ 确认隐私由架构保障而非承诺保障
  └─ 输出: 信封格式定义 + 隐私边界图

Step 5: 流水线固化
  └─ 定义规则过滤→价值argmax→意图承诺
  └─ 确认全流程为确定性纯函数
  └─ 输出: 决策流程图 + 测试向量

Step 6: 成本锚定
  └─ 定义误报/漏报成本比，推导阈值
  └─ 绘制伪造成本曲线
  └─ 输出: 成本矩阵 + 经济模型

Step 7: 退化路径设计
  └─ 回答"当X假设失效时，系统退化成什么"
  └─ 设计嵌套兜底机制
  └─ 输出: 退化层级图 + 兜底策略
```

### 3.2 问题诊断速查表

| 症状 | 可能违反的公理 | 诊断动作 |
|---|---|---|
| 系统需要全局协调 | 1.1 无全局假设 | 检查是否可用因果偏序+局部权威替代 |
| 数据被修改/删除 | 1.2 事实终局 | 转换为追加事件模式 |
| 算力成本随规模爆炸 | 1.3 惰性计算 | 检查沉睡机制与订阅分级 |
| 隐私依赖合规承诺 | 1.4 基础设施中立 | 改为未订阅即不下发的架构 |
| 运行时仲裁复杂 | 1.5 确定性流水线 | 前移到设计时消除冲突 |
| 安全依赖绝对加密 | 1.6 成本驱动 | 改为伪造经济学约束 |
| 假设当代软件永存 | 1.7 时间分层 | 设计退化链与自描述机制 |

---

## 4. 反模式 (Anti-Patterns)

以下模式在本技能框架下被视为**设计缺陷**，必须避免：

1. **全局FIFO队列**: 要求全局共识与全局"先来后到"，违反公理1.1
2. **可回撤事务**: 允许删除或修改已结算记录，违反公理1.2
3. **全真模拟**: 试图镜像整个世界，违反公理1.3
4. **内容感知路由**: 基础设施解析payload，违反公理1.4
5. **运行时仲裁器**: 冲突依赖外部仲裁，违反公理1.5
6. **绝对安全承诺**: 声称"不可能伪造"，违反公理1.6
7. **单一格式依赖**: 假设当代格式永不过时，违反公理1.7
8. **隐式状态切换**: 边界切换无显式状态机，违反技能2.5
9. **逐类手调阈值**: 置信度阈值凭经验设定，违反技能2.7
10. **单层过滤**: 传感器数据直达决策环，违反技能2.9

---

## 5. 快速参考卡

### 5.1 术语链（全文统一）
```
感知样本（感知日志，非真源）
  → 迁移沿候选（决策环输入）
  → 预演（REHEARSAL，内存态）
  → 事实（FACT，事件日志记录）
```

### 5.2 订阅分级 (P0-P3)
| 级别 | 触发 | 内容 | 频率 |
|---|---|---|---|
| P0 占位 | 索引命中 | 位置+摘要 | 秒级 |
| P1 轮廓 | 邻近/弱相关 | 平滑状态 | 1-5Hz |
| P2 完整 | 交互/可能干涉 | 完整状态 | 10-20Hz |
| P3 协同 | 实时配合/操控 | 输入级 | 逐帧 |

### 5.3 时钟权威矩阵
| 判定类型 | 权威 | 处置 |
|---|---|---|
| 域内结算顺序 | 本域逻辑时钟 | 物理卡顿记偏差事实 |
| 网络超时/断链 | 本域物理时钟 | 不参与他域结算 |
| 共享资源竞争 | 资源所在cell权威 | 一次定序即终局 |
| 并发且无权威冲突 | Vector Clock + 确定性tie-break | 纯函数，全节点一致 |

### 5.4 软逻辑流水线
```
全部候选计划 P
  → [规则] 合法性过滤：P_legal = { p ∈ P | rules(p) = VALID }
  → [价值] 效用排序：argmax_{p ∈ P_legal} utility(p)
  → [意图] 承诺执行：选定计划进入承诺窗口（带迟滞，防振荡）
```

### 5.5 阈值公式
```
θ = C_fn / (C_fp + C_fn)
```
争论从"拍脑袋定0.95"变为"确认误刹车一次亏多少钱"

---

## 6. 边界声明

本技能**不承诺解决**以下问题：
- 内容与应用合规（属运营与治理层）
- 法律责任认定（接口预留于授权协议与事实档案）
- 跨平台身份互通的政治经济学
- 关键基础设施接入的行业监管
- 数据资产跨国税务与继承衔接
- 切片期权市场流动性不足等市场尾部风险

本技能保持**中立**，只提供架构与协议层面的约束与证据基础设施。

---

## 7. 版本历史

- v1.0 (2026-08-15): 初始版本，基于世界模型/空间互联网/HESP/奇普计划四文档的方法论提炼

---

> **结语**: 数字保存的深层困境不是"比特能存多久"，而是"意义能活多久"。本技能的目标是让系统设计者在面对复杂问题时，能够自觉运用"域架构思维"——不假设全局权威，不追求绝对真理，不预付全真成本；用显式规则、因果偏序、经济约束和自描述机制，在物理极限内构建可审计、可退化、可存活的系统。
