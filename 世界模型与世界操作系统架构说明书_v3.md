# 世界模型与世界操作系统架构说明书

> **版本**: v3.0
> **定位**: 面向多智能体、高动态、可审计的符号化世界系统
> **v3 核心修正**:
> 1. 事件流升格为唯一真源（Single Source of Truth），其余三棵树为其物化视图
> 2. 新增不确定事件机制（pending / retract），解决概率性感知输入的回撤问题
> 3. 时空树历史策略定基线：定距快照 + 事件增量
> 4. 新增多时钟域仲裁协议（时钟权威矩阵 + 漂移处置）

---

## 1. 架构愿景

本架构旨在构建一个**可精确操作、可因果审计、可长期演化**的数字世界系统。它不追求用单一模型"理解一切"，而是将世界的不同维度解耦为专门的数据结构与运行时，让生成式AI（LLM/神经场）与符号系统各尽其职。

**关键认知**:
- **世界模型（World Model）** = 世界的"数据库 + 本体论"，回答"是什么、在哪里、发生了什么"。
- **世界操作系统（World OS）** = 世界的"运行时 + 事务系统"，回答"如何演化、能否发生、为何如此"。
- **LLM/生成式模型** = "翻译官与感知器"，连接人类意图与符号宇宙，但不直接操作原子状态。

---

## 2. 总体架构

### 2.1 三层架构

```
┌─────────────────────────────────────────────────────────────┐
│  L2 认知接口层（Cognitive Interface）                        │
│  ├─ LLM 自然语言理解（意图解析、目标消歧、计划生成）          │
│  ├─ 生成式场模型（NeRF/3DGS/神经信道预测，连续场推断）        │
│  └─ 感知表征器（视频/点云/传感器 → 带置信度的候选表征）       │
└──────────────────────────┬──────────────────────────────────┘
                           │ 符号化指令 / 场数据 / 不确定事件
┌──────────────────────────▼──────────────────────────────────┐
│  L1 世界模型层（World Model）— 符号记忆层                    │
│  ├─ 事件因果图（唯一真源，append-only 事件流 + 因果DAG）     │
│  └─ 三大物化视图（可派生、可重建）：语义 / 实体 / 时空        │
│  └─ 三大业务逻辑系统（软逻辑）：规则 / 意图 / 价值            │
└──────────────────────────┬──────────────────────────────────┘
                           │ 事件 / 状态变更 / 规则校验
┌──────────────────────────▼──────────────────────────────────┐
│  L0 世界操作系统层（World OS）— 运行时层                       │
│  ├─ 时钟调度器（多时钟域 + 时钟权威矩阵 + 漂移仲裁）          │
│  ├─ 事件总线（原子队列 + 因果DAG + 反事实沙盒 + 回撤通道）    │
│  ├─ 执行引擎（规则 / 物理 / 行为 / 叙事）                     │
│  ├─ 状态管理器（事件流即WAL + 定距快照 + 分支）              │
│  └─ 接口网关（LLM适配器 / 人类接口 / 外部系统API）            │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 世界模型层（L1）

### 3.1 架构总览

| 层级 | 组件 | 本质 | 核心问题 | 数据形态 |
|------|------|------|---------|---------|
| **真源** | 事件因果图 | append-only 事件流 | 发生了什么？为什么？ | 因果DAG + 概率 + 反事实 + 回撤记录 |
| **物化视图** | 语义图谱树 | 知识图谱 | 是什么？意味着什么？ | 本体+规则，有向图 |
| | 实体搜索树 | 索引结构 | 是谁？当前状态？ | 实体实例，HashMap/B+树 |
| | 时空索引树 | 时空索引 | 在哪里？何时？轨迹？ | 4D索引 + 定距快照 + 事件增量 |
| **业务逻辑系统** | 规则/约束系统 | 规则引擎 | 能做什么？绝对不可？ | 约束集 + 执行器 |
| | 意图/行为系统 | 目标+计划+状态机 | 想做什么？如何达成？ | 目标层次 + 计划队列 |
| | 价值/情感系统 | 效用+情感状态机 | 什么值得？偏好排序？ | 效用向量 + 情感状态 |

**v3 关键区分（三态分离）**:
- **事件流是真源（Source of Truth）**：世界上唯一不可重建的数据。一切状态变更以事件形式 append-only 写入。
- **三棵树是物化视图（Materialized View）**：语义/实体/时空树全部由事件流投影（projection）生成，损坏或丢失可从事件流 + 快照重放重建。
- **三大业务逻辑系统是运行时（Runtime）**：瞬态、可重算，消费视图、产出新事件。

这一修正解决了 v2 遗留的**跨树事务问题**：v2 中一个事件需要同时写事件树、时空索引、实体树，是事实上的跨四树分布式事务。v3 将其转化为**单点写入 + 异步投影**：

```
写入路径：  决策 → 规则校验 → 事件流 append（唯一原子写点）→ ACK
读取路径：  事件流 → 投影器（Projector）→ 三棵视图树（最终一致，延迟可控）
```

由于事件 append 是唯一的原子写点，崩溃恢复等价于"从最近快照 + 后续事件重放"，WAL 与事件流天然合一，不再需要独立的事务协调器。

---

## 4. 事件因果图（唯一真源）

### 4.1 事件节点

```json
{
  "eventId": "Evt-7723",
  "eventType": "WEAPON_DRAWN",
  "actorId": "Entity-42",
  "relatedEntities": ["Entity-7"],
  "spatiotemporalRef": "Anchor-7@169...",
  "causes": ["Evt-7701"],
  "effects": ["Evt-7724", "Evt-7725"],
  "status": "CONFIRMED",
  "confidence": 1.0,
  "provenance": { "source": "perception/cam-3", "modelVersion": "v2.1" },
  "clockStamp": { "physical": 1690000123.456, "logical": 1042, "vector": [12, 3, 41] },
  "counterfactuals": []
}
```

### 4.2 不确定事件机制（Pending / Retract）

**问题背景**：感知层是概率性的。一个置信度 0.6 的"疑似拔武器"事件，若按确定事件直接生效，误报后其下游因果（NPC 已切换意图、价值系统已扣信任度）将无法干净回收。这是符号系统接入概率输入时最硬的问题。

**机制设计**：事件带 `status` 字段，生命周期为三态：

```
PENDING（待定） → CONFIRMED（确认）
               ↘ RETRACTED（回撤）
```

- **PENDING 事件的受限传播**：
  - 写入事件流（真源必须记录"曾经有过这个候选"），但**不触发投影器更新实体/时空视图**。
  - 业务逻辑系统可读取 PENDING 事件做**试探性反应**，但反应本身也以 PENDING 事件写入，形成"待定因果链"。
  - 规则系统对 PENDING 事件只做**预校验**（若确认后是否合法），不执行状态变更。
- **CONFIRMED**：达到确认条件（第二感知源佐证 / 超时窗口内无否决 / 置信度超过规则系统设定的阈值），整条待定因果链原子转正，投影器按事件序重放。
- **RETRACTED**：候选被否证。回撤不是删除（真源 append-only 不可破坏），而是：
  1. 追加一条 `RETRACTION` 事件，指向被回撤事件；
  2. 投影器遇到 RETRACTED 事件时**跳过其投影**；
  3. 已由其触发的下游 CONFIRMED 事件，沿因果边递归标记 `INVALIDATED`，并生成 `COMPENSATION` 事件（如"NPC-3 恢复巡逻意图"）——补偿是显式事件而非隐式回滚，保证审计链完整。

**置信度阈值策略**（写入规则系统）：

| 置信度 | 处置 |
|--------|------|
| ≥ 0.95 | 直接 CONFIRMED（低风险感知：近距离清晰视觉） |
| 0.6–0.95 | PENDING，等待佐证或 200ms 超时自动确认 |
| < 0.6 | 不进入事件流，仅记录在感知日志（非真源） |

阈值可按事件类型分级：`SIGNAL_DEGRADED` 类事件宁可误报（阈值低），`WEAPON_DRAWN` 类事件宁可漏报（阈值高）。

### 4.3 反事实沙盒

- 从检查点克隆隔离世界副本，修改/删除目标事件，由物理/行为引擎重新推演。
- 结果只读、不回流主世界。
- 混沌系统（电磁干扰等）的反事实输出是**概率分布**：记录为 `CounterfactualNode`，附 `resultDistribution` 与 `confidence: statistical`。

---

## 5. 三大物化视图

### 5.1 语义图谱树（Ontology Tree）

- **职责**: 定义世界中的概念、类型、属性及推理规则。
- **数据形态**: 有向图（RDF/属性图）。
- **关键设计**:
  - 与实体树解耦：语义树存"`AttackEvent` 是 `CombatAction` 的子类"，实体树存实例状态。
  - 支持规则推理："所有 `CombatAction` 可能触发 `Aggro` 状态"。
- **静态性**: 运行时只读，变更以 `ONTOLOGY_CHANGE` 事件写入事件流，版本化热更新。

### 5.2 实体搜索树（Entity Tree）

- **职责**: 管理世界中的全部可辨识对象（人、物、组织、抽象概念实例）。
- **数据形态**: 以 `entityId` 为主键的索引结构（HashMap / B+树），由事件流投影维护。
- **示例**:
  ```json
  {
    "entityId": "NPC-7",
    "name": "哨兵",
    "typeRef": "Guard",
    "spatialAnchorId": "Anchor-Block-A",
    "state": { "health": 100, "posture": "alert", "trust": { "Entity-42": 0.3 } },
    "lastEventId": "Evt-7723"
  }
  ```
- **视图特性**: 每个实体记录 `lastEventId`，支持增量重建与一致性校验（视图落后真源多少事件可查）。

### 5.3 时空索引树（Spatiotemporal Tree）

- **职责**: 索引实体在时空中的位置与轨迹，支持范围查询、邻近查询与 `AS OF t` 时间旅行查询。
- **数据形态**: 分层空间索引（八叉树/27叉树/R-tree）+ 锚点系统 + **定距快照 + 事件增量**。

**v3 定基线：历史状态的混合存储策略**

v2 遗留问题："事件回溯**或**显式快照"是两种成本模型（读时重算 vs 写时存快照），不定基线则 `AS OF t` 查询延迟不可控。v3 定为：

```
AS OF t 查询 = 最近的前置快照 S(t₀ ≤ t) + 重放 (t₀, t] 区间内的事件增量
```

- **快照定距**: 默认每 N=1000 事件或每 T=60s（先到为准）生成一次时空视图快照。快照只存**发生变化的空间区域**（增量快照，借鉴写时复制），不是全量序列化。
- **重放上限**: 任意 `AS OF t` 查询的重放量 ≤ 1000 事件，查询延迟有硬上界（实测目标 < 50ms）。
- **密度自适应**: 事件高发区域（主城）自动缩短快照间隔，死区（无人星系）拉长间隔——快照策略本身是规则系统可配置的策略，不是编译期常量。
- **锚点系统（Anchor）**: 支持多坐标系（直角/球面/极坐标/运动坐标系）。锚点本身可被索引管理，支持跨场景切换；锚点变更以事件形式进入真源。
- **边界条件索引**: 对于连续场（如电磁场），不存储场数据，只存储影响场的**离散实体**（信号源、障碍物）及其参数。

---

## 6. 业务逻辑系统（三大软逻辑）

三大系统不是树结构，而是**运行时的执行逻辑**。它们消费视图、产出事件。

### 6.1 规则/约束系统（Rules & Constraints System）

- **本质**: 规则引擎 + 约束求解器。
- **职责**: 定义世界的绝对边界，是**合法性的最终仲裁者**。
- **分层**:
  - **物理规则**: 速度上限、能量守恒、空间曲率限制。
  - **系统规则**: 技能CD、资源公式、阵营声望机制。
  - **社会规范**: 贸易协议、宣战规则、外交礼仪。
- **不可妥协性**: 100% 确定性的符号系统。LLM可辅助生成规则DSL初稿，运行时零LLM参与。
- **v3 新增职责**:
  - 感知事件的**置信度阈值表**（见 4.2）。
  - PENDING 事件的**预校验**与超时窗口定义。
  - 时钟域**权威矩阵**（见 8.1.1）的策略配置。

### 6.2 意图/行为系统（Intention & Behavior System）

- **本质**: 目标层次结构 + 计划生成器 + 状态机。
- **分层**:
  - **战略层**（分钟/小时级）: 可由LLM生成策略模板。
  - **战术层**（秒级）: 显式状态机执行，保证确定性。
  - **操作层**（帧级）: 绝对禁止LLM介入，纯符号状态机。
- **与真源的关系**:
  - 意图对象是**瞬态运行时对象**，不直接持久化；只有关键决策（目标切换）才作为事件写入事件流。
  - v3 修正：对 PENDING 事件的反应意图同样标记为试探性（`speculative: true`），随源事件一并确认或回撤。

### 6.3 价值/情感系统（Affect & Value System）

- **本质**: 效用函数 + 情感状态机。
- **数据形态**:
  - 显式数值 `trust=0.73`：体现在实体视图（由情感事件投影）。
  - 情感变化规则：语义树（"背叛导致信任度下降0.5"）。
  - 情感变化事件：事件流（真源）。
- **v3 修正**: 由 PENDING 事件引发的价值变化**不立即投影**到实体视图——NPC 不会因为你"疑似拔武器"就真的掉信任，只会生成试探性警觉意图。确认后信任度才正式变更；回撤则零成本。这条避免了误报污染长期社交状态。
- **与LLM的边界**: LLM负责将数值变化翻译为自然语言解释；价值冲突时可提供仲裁建议，最终权重调整以事件写入。

---

## 7. 感知表征层（L2）

感知层负责将原始输入（视频、点云、传感器、自然语言）转化为**带置信度的候选表征**。

| 感知器 | 输入 | 输出 | 技术形态 |
|--------|------|------|---------|
| **空间感知器** | 视频/点云/传感器流 | 3D场景图、占据网格、NeRF/Gaussian | 生成式视觉模型 |
| **时序感知器** | 连续帧/事件流 | 动作片段、关键帧、运动轨迹 | 视频理解模型 |
| **语义感知器** | 文本/语音/交互日志 | 结构化事件描述、意图标签 | LLM/NLP pipeline |
| **关系感知器** | 多实体交互画面 | 接触图、社交距离、势力范围 | 图神经网络 |

**关键原则**: 感知层是**概率性的、有损的**。输出一律携带 `confidence` 与 `provenance`（来源、模型版本），按 4.2 的阈值策略决定以 CONFIRMED 还是 PENDING 进入事件流。

---

## 8. 世界操作系统层（L0）

### 8.1 时钟调度器（Clock Scheduler）

世界不是单一时间线。支持多时钟域及其映射：

| 时钟域 | 粒度 | 用途 | 示例 |
|--------|------|------|------|
| **物理时钟** | 毫秒级 | 网络同步、超时判定、技能前摇 | "0.5秒前摇" |
| **逻辑时钟** | 离散tick | 状态帧推进、回合制结算 | "第3到第5逻辑帧" |
| **叙事时钟** | 弹性 | 情节节奏、章节推进 | "三年过去了" |
| **因果时钟** | Lamport/Vector | 分布式多智能体的happened-before判定 | 冲突仲裁 |

#### 8.1.1 时钟权威矩阵与漂移仲裁（v3 新增）

**问题背景**："技能前摇 0.5 秒"（物理时钟）与"占用第 3–5 逻辑帧"（逻辑时钟）在服务器卡顿一帧时发生漂移——无敌盾和致死技能同一窗口结算，谁先生效？这是游戏行业经典的一致性难题的世界模型版本。v2 只有时钟映射示例，没有仲裁协议。

**仲裁原则：每类判定有且只有一个权威时钟域**，由规则系统配置：

| 判定类型 | 权威时钟 | 理由 | 漂移处置 |
|----------|---------|------|---------|
| 技能前摇/后摇结算 | **逻辑时钟** | 帧级确定性，可回放复现 | 物理时长仅作表现层插值，卡顿导致物理时长不足时**表现加速补帧**，结算不变 |
| 网络超时/断线判定 | **物理时钟** | 墙钟真实流逝 | 不参与逻辑结算 |
| 同时性冲突（同帧互斥技能） | **因果时钟** | 无全局物理同时性，只有happened-before | Vector Clock 不可比较（并发）时，进入**确定性 tie-break**：比较 (优先级, actorId字典序)，全节点结果一致 |
| 叙事时间推进 | **叙事时钟** | 弹性，服务情节 | 单向映射，不反向约束其他时钟 |

**两条铁律**:
1. **结算只依赖逻辑帧序与因果序，绝不依赖物理时长**。物理时钟卡顿改变的是表现层（动画插值、补帧），不改变结算结果——这是"可回放"的前提。
2. **并发冲突的 tie-break 必须是确定性的纯函数**，输入为事件本身（优先级、ID），禁止引入节点本地状态（如本地时钟、随机数，除非随机种子来自事件流真源）。

### 8.2 事件总线（Event Bus）

世界状态变更的唯一入口。

```
感知输入 / 智能体决策 → 置信度分级（4.2） → 事件总线 →
规则系统校验（CONFIRMED 全校验 / PENDING 预校验） →
事件流 append（唯一原子写点） →
投影器异步更新三棵视图 → 通知订阅者
```

- **原子事件队列**: FIFO，严格时序。
- **因果事件图**: DAG，支持分支/合并。
- **回撤通道**: RETRACTION / COMPENSATION 事件的专门处理路径（见 4.2）。
- **反事实沙盒**: 隔离推演，不写入主世界。

### 8.3 执行引擎（Execution Engine）

四引擎并行，优先级严格：

| 引擎 | 输入 | 输出 | 优先级 | LLM可否介入 |
|------|------|------|--------|------------|
| **规则引擎** | 待执行事件 + 规则系统 | 合法/非法，参数修正 | **最高** | ❌ 绝对禁止 |
| **物理引擎** | 合法事件 + 时空视图 | 新状态（位置/速度/形态） | 高 | ⚠️ 生成式模型可替代场计算，但接口不变 |
| **行为引擎** | 实体状态 + 意图系统 | 下一动作/目标调整 | 中 | ⚠️ 高层策略可LLM生成，执行层禁止 |
| **叙事引擎** | 事件流 + 价值系统 | 情节节奏、视角切换 | 低 | ✅ 可由LLM驱动 |

**冲突解决**: 行为引擎想"NPC穿墙"，规则系统返回 `INVALID: collision constraint violated`。

### 8.4 状态管理器（State Manager）

v3 重构：事件流即 WAL，二者合一。

- **真源**: append-only 事件流（分段存储 + 索引），崩溃后唯一需要保护的数据。
- **视图缓存**: 三棵树为内存热数据 + 磁盘物化，损毁可重建。
- **快照**: 定距增量快照（见 5.3），恢复 = 加载最近快照 + 重放后续事件。
- **分支（Branch）**: "世界线分叉" = 从事件流的某个 eventId 开始分叉新的事件流分支，两分支共享前缀（写时复制），用于A/B测试或平行宇宙。
- **重建演练**: 定期执行"从事件流全量重建视图并与现网视图比对"的一致性校验（混沌工程式自检）。

### 8.5 接口网关（Interface Gateway）

- **LLM适配器**: 自然语言 ↔ 符号操作（JSON/Protobuf）。
- **人类接口**: 可视化、调试、手动注入事件（注入事件同样走事件总线，无特权后门）。
- **外部系统API**: 与其他世界或现实系统互联。

---

## 9. 连续系统的处理（以电磁场为例）

电磁场是连续、混沌的偏微分方程系统，**不进入符号树**。符号树只管理场中的**离散实体、事件与决策**。

### 9.1 分层处理

```
┌─────────────────────────────────────────┐
│  连续物理层（Field Engine）              │
│  传统：FDTD / 射线追踪                   │
│  现代：神经场模型 / 生成式信道预测        │
│  输出：场强分布、多径延迟、信噪比热力图   │
│  性质：混沌、连续、概率性                 │
└─────────────┬───────────────────────────┘
              │ 离散化接口（Field-to-Event Gateway）
┌─────────────▼───────────────────────────┐
│  符号层                                   │
│  只存"场发生质变"的离散事件和实体关系     │
└─────────────────────────────────────────┘
```

### 9.2 符号层的具体职责

| 视图/系统 | 存储内容 | 不存储内容 |
|---|---|---|
| **时空索引树** | 信号源位置/参数、障碍物几何/材质 | 每一点的场强值 |
| **实体搜索树** | 通信设备（手机/基站/无人机）的协议/频段/灵敏度 | 实时接收信号质量 |
| **事件流** | `LINK_ESTABLISHED`, `SIGNAL_DEGRADED`, `HANDOVER` | 场强渐变过程 |
| **语义图谱树** | `5GHz` `is-a` `微波`, `钢筋混凝土` `causes` `衰减` | 具体衰减数值 |

### 9.3 场-事件转换器（Field-to-Event Gateway）

物理引擎每帧输出场数据，网关只在**观测点**采样并检测阈值跨越：

```
采样位置：通信设备接收端、预设监测点
阈值检查（v3：阈值跨越事件一律带置信度，按 4.2 分级）：
  - RSSI 跨越 -90dBm？ → SIGNAL_DEGRADED（低阈值，可直接CONFIRMED）
  - SNR 从 >20dB 跌到 <10dB？ → INTERFERENCE_DETECTED
  - 多径延迟超过均衡器容忍？ → LINK_UNSTABLE
事件写入事件流 → 触发后续逻辑（切换基站/启动抗干扰/标记盲区）
```

### 9.4 反事实与混沌

对于"如果移除Building-15，信号如何？"：

- 物理引擎（传统或生成式）重跑仿真。
- 结果是**概率分布**，不是确定性因果链。
- 事件流记录为 `CounterfactualNode`，附带 `resultDistribution` 与 `confidence: statistical`。

**符号层承认混沌的存在**，但不试图用符号结构模拟场本身。

---

## 10. 生成式模型融合

### 10.1 生成式模型替代物理引擎

生成式模型（NeRF/3DGS/神经信道预测）正在替代传统场计算的"白箱方程"，但**接口边界不变**。

```cpp
// 旧接口：确定性物理引擎
class PhysicsEngine {
    Field computeField(Scene scene, Source source);
}

// 新接口：生成式场模型
class GenerativeFieldModel {
    ProbabilisticField inferField(Scene scene, Source source);
    // 输出的是场强分布，不是确定值
}

// 不变：场到事件的离散化网关
class FieldEventGateway {
    Event[] sampleEvents(ProbabilisticField field, List<Entity> observers);
}
```

### 10.2 生成式模型本身也需要被符号层管理

| 管理内容 | 存储位置 | 原因 |
|----------|---------|------|
| 模型版本号 | 实体视图/语义树 | "v2.1在5GHz场景下比v1.7更准确" |
| 训练数据范围 | 语义树 | "该模型只在城市密集区训练过" |
| 置信度阈值 | 规则系统 | "输出方差>0.3时降级为传统射线追踪" |
| 预测归因 | 事件流 `provenance` | "本次链路中断预测由模型A做出" |

### 10.3 LLM 的精确位置

LLM不是世界模型本身，而是**世界模型的操作员和翻译官**。

```
用户输入："让NPC-7去火星基地"
    ↓
[LLM适配器] 解析为符号指令
    ↓
{ actor: "NPC-7", action: "MOVE", target: "Anchor-Mars-Base" }
    ↓
[世界OS] 规则校验 → 物理推演 → 事件流append → 视图投影
    ↓
[LLM叙事层] 生成描述："火星的沙尘暴比预计的早到了"
    ↓
输出层：渲染/文本/音效
```

**铁律**: LLM生成的计划、对话、描述，必须经过**规则系统校验**后才能生效。LLM可以建议，但规则系统拥有**最终否决权**。

---

## 11. 数据流全景（含不确定事件）

一个完整的"智能体行动"在世界中的流动（v3 版，含 PENDING 路径）：

```
[感知层] 摄像头/传感器/用户输入
    ↓
"疑似玩家A在坐标X拔出了武器，置信度0.72"
    ↓
[LLM适配器/感知器] 解析为候选事件，按4.2分级 → PENDING
    ↓
[事件总线] 规则系统预校验：该区域允许拔武器？是 → 待定因果链建立
    ↓
[行为系统] 试探性反应：NPC-3 生成 speculative 意图"隐蔽观察"（不写实体视图）
    ↓
200ms内：[感知层] 第二视角摄像头佐证，置信度升至0.96
    ↓
[事件总线] Evt-7723 CONFIRMED，待定因果链原子转正
    ↓
[投影器] 实体视图同步（武器=拔出）→ 时空视图刷新 → 价值变化投影（信任度-0.2）
    ↓
[状态管理器] 事件流已持久化，视图最终一致
    ↓
[LLM叙事层] 生成描述："你拔出枪的瞬间，街角的守卫猛地缩回了阴影里"
    ↓
[输出层] 渲染画面 / 返回文本 / 触发音效

（反路径）若第二视角否证：Evt-7723 → RETRACTED，
追加 RETRACTION 事件，NPC-3 的 speculative 意图随链失效，
价值系统零污染，审计链完整可查。
```

---

## 12. 演进路线

| 阶段 | 目标 | 关键动作 |
|------|------|---------|
| **P0 真源** | 事件流跑通：append + 投影器 + 单视图重建验证 | 先做事件流与实体视图两棵，验证"真源→视图"范式 |
| **P1 骨架** | 世界可查询、可定位、可回溯 | 三棵视图齐备；定距快照 + AS OF t 查询达标 |
| **P2 约束** | 世界有边界 | 规则系统（硬约束）+ 时钟权威矩阵生效 |
| **P3 感知** | 世界能看懂输入、扛得住误判 | LLM适配器 + 生成式场模型 + PENDING/RETRACT 全链路 |
| **P4 能动** | 世界有自主性 | 意图/行为系统 + 价值/情感系统 |
| **P5 并行** | 世界可分叉 | 事件流分支与平行宇宙 |
| **P6 互联** | 世界可对外连接 | 接口网关对接外部系统与真实世界 |

**v3 对路线图的调整说明**：v2 的 P0 要求"四棵树同时跑通"，实际是要求一次建成四个相互耦合的系统。v3 将 P0 收敛为"事件流 + 一个投影器 + 一个视图"的最小闭环，先验证"真源→视图"这个全文最关键的架构假设，再横向扩展视图数量。这显著降低了首期实现风险。

---

## 附录A：核心设计原则

1. **事件流是唯一真源**: 世界上唯一不可重建的数据是事件流；三棵视图树均可由事件流 + 快照重放派生。真源 append-only，回撤以 RETRACTION 事件追加，永不物理删除。
2. **不确定事件显式化**: 概率性感知的输入以 PENDING 状态进入真源，确认/回撤都是显式事件；下游试探性反应随链同生共死，误报不污染长期状态。
3. **确定性底座**: 任何涉及状态变更、规则校验、因果回溯的模块，必须是符号化的、可审计的。
4. **结算权威唯一**: 每类判定有且只有一个权威时钟域；并发冲突的 tie-break 是确定性纯函数。物理时钟只影响表现，不影响结算。
5. **LLM不碰状态**: LLM可以建议、解释、生成，但世界状态的最终写入权只属于世界OS。
6. **混沌封装**: 连续系统的混沌性被封装在物理引擎/生成式模型的黑箱内，符号层只消费其离散跃迁事件；混沌系统的反事实输出是概率分布。
7. **软逻辑可渐进**: 规则、意图、价值是业务逻辑系统，初期可用脚本/LLM模拟，需要精确控制时再硬化。

---

## 附录B：v3 变更摘要

| # | 问题 | v2 状态 | v3 处置 |
|---|------|---------|---------|
| 1 | 跨树事务粒度 | 未提，隐含跨四树分布式事务 | 事件流为唯一原子写点，三树降级为异步物化视图，WAL 与事件流合一 |
| 2 | 概率感知输入 | 感知层带置信度，但进入符号层后无下文 | PENDING/CONFIRMED/RETRACTED 三态生命周期 + 补偿事件 + 置信度阈值分级表 |
| 3 | 时空树历史策略 | "回溯或快照"二选一未定 | 定基线：定距增量快照（N=1000事件/T=60s，密度自适应）+ 事件增量重放，AS OF t 延迟有硬上界 |
| 4 | 多时钟仲裁 | 仅有映射示例 | 时钟权威矩阵：结算权归逻辑/因果时钟，物理时钟只管表现；并发冲突用确定性 tie-break |
