# 空间实时交互网络架构设计文档

> 版本：v1.0（行业版）
> 定位：面向下一代网络（6G/空间互联网）的实时交互基础设施架构。本文档不含任何特定应用场景的假设，适用于远程呈现、大型活动、工业数字孪生、智慧城市、扩展现实（XR）等一切"多主体实时空间交互"场景。
> 读者：通信设备商、运营商、标准组织的技术决策层。

---

## 1. 问题陈述：现有互联网为什么承载不了实时空间交互

### 1.1 内容互联网的天花板

过去四十年的互联网是一套**内容分发**架构：TCP/IP 路由字节，HTTP/CDN 分发内容，其设计假设是"内容生产一次、消费多次、秒级延迟可接受"。

而正在到来的应用——XR 远程呈现、千人级实时活动、城市级数字孪生、车路协同——提出了完全不同的需求：

- **状态实时性**：交互双方需要毫秒级感知彼此的状态变化，而非获取一份静态内容；
- **主体海量性**：单场景并发交互主体从百（视频会议）跃迁至十万（大型活动）乃至亿级（城市孪生）；
- **内容无限异构**：同一物理空间上叠加的应用类型不可预知，基础设施不能为每种应用定制；
- **体验个体化**：每个参与者看到的必须是"以他为中心"的连续、自洽的世界，而非统一的广播流。

### 1.2 现有技术路线的死胡同

目前业界的应对方式本质是"**把实时渲染服务端化**"：建立一个统一的世界模拟器，全局时钟驱动，按区域分片，向终端推流。这条路有无法逾越的天花板：

- **全局时钟瓶颈**：世界的推进速度受限于最慢的分片，算力需求随世界规模平方增长；
- **统一格式瓶颈**：基础设施必须理解内容语义才能模拟它，新应用类型要求平台级改造；
- **规模与密度矛盾**：世界越大，人越稀疏，实时交互的价值越低——这是所有"大空间"产品共同的死因。

### 1.3 本文档的主张

> **不要建造一个更大的世界模拟器。把网络从"内容路由"升级为"在场（Presence）路由"：基础设施不理解、不模拟、不仲裁任何内容，只负责把"谁的状态变化该送达谁"这件事做到极致。**

这套架构在结构上是互联网的自然延伸——正如万维网统一了内容，它统一的是**实时在场**。游戏是它的第一个验证场景，但其形态本质上是互联网的空间化版本。

---

## 2. 设计哲学

### 2.1 核心世界观：一人一个会话域

- 每个用户拥有一个**个人会话域（Session Domain）**，以他自己的节奏运行。他是域内唯一的一等主体。
- 域内其余一切皆为**在场代理（Presence Agent）**：他人是真实主体经网络路由而来的代理，虚拟主体（数字人、设备、传感器数字镜像）由服务侧驱动。两者在协议上完全同构。
- **不存在"世界"这个实体。** 存在的只有：纯数学的索引（寻址）、用户各自的会话域（体验现场）、网络侧保管的权威记录（事实档案）。用户感受到的"共同空间"，是各方体验被同一套事实档案校准后的产物。

### 2.2 三条元原则

1. **体验连续性优先于数据真实性。** 系统交付的不是全局客观真相，而是每个参与者各自自洽、各自认可的因果序列。绝对同时性在分布式物理中不存在，架构不追求它。
2. **已呈现不可推翻。** 终端已经呈现给用户的因果不得被系统作废。交互规则必须保证：绝大多数交互结果在双方的体验里都成立。
3. **存在即被感知。** 无观察者的实体退化为潜在（索引条目 + 极低频自更新），算力消耗正比于被观察密度。

### 2.3 与内容互联网的对照

| 维度 | 内容互联网（TCP/IP + Web） | 本架构（在场网络） |
|---|---|---|
| 路由对象 | 内容（文档、音视频流） | 在场（实时状态事件） |
| 内容理解 | 不理解（字节透明） | 不理解（事件透明） |
| 时钟 | 无（异步） | 无全局时钟，实体各自节奏 |
| 会话 | 请求-响应 | 持续订阅 |
| 一致性 | 最终一致 | 逐用户体验自洽 + 事实档案终局 |
| 扩展方式 | 加缓存、加带宽 | 加索引、加路由 |

---

## 3. 架构公理

1. **纯数学底座**：最底层只有时空坐标系与索引，无实体、无模拟、无业务语义。
2. **实体自主节奏**：每个实体有自己的更新周期——工业设备可以一年一次，行人可以每秒 20 次。无全局同步屏障。
3. **事件是唯一流通物**：实体间只交换"事件"（带信封的不透明字节流），网络层不理解内容。
4. **端到端语义**：语义只存在于端点，网络只管送达、有序、不篡改。
5. **一人一域，域不合并**：每个用户永远是自己域内唯一的一等主体，他人皆为代理。订阅替代合并（唯一例外：毫秒级强耦合交互临时建立协同上下文）。
6. **三级参与**：实体间关系分三级——订阅（接收事件）→ 同步（共享时钟）→ 协同（联合裁决）。默认停在最低级，成本逐级跳升。
7. **事实终局**：交互结果由服务侧实体一次结算即成事实（结果直送为常态、临时协同为例外），各方域各自呈现，不回溯、不改写。
8. **规则可交换化**：凡时序模糊下结果对立的交互，业务规则必须允许双生效或宽限窗口，使结果与顺序无关。
9. **惰性计算**：未被订阅的实体不模拟，未被观看的细节不计算。与观察者已有认知不矛盾，是模拟的唯一约束。
10. **分层依赖不可逆**：慢层（数学/传输）不依赖快层（应用/呈现）。

---

## 4. 系统分层

```
┌───────────────────────────────────────────────┐
│ L4  呈现层（终端侧）                            │
│     终端 = 事件的听众，把事件流重建为可感体验      │
├───────────────────────────────────────────────┤
│ L3  应用层（租户相关，数据驱动）                  │
│     业务规则 = 应用协议 + 规则原语装配            │
├───────────────────────────────────────────────┤
│ L2  编排层（服务侧智能）                         │
│     虚拟主体驱动、事件编排、缺席补全、记录对齐     │
├───────────────────────────────────────────────┤
│ L1  实体层                                    │
│     自主节奏的进程、时间线环形缓冲、一次结算       │
├───────────────────────────────────────────────┤
│ L0  传输层                                    │
│     信封路由：送达、通道内有序、不篡改            │
├───────────────────────────────────────────────┤
│ L-1 时空数学层                                 │
│     坐标系 + 时空索引 + 关系索引（社交/组织图）    │
└───────────────────────────────────────────────┘
```

关键性质：**L-1 与 L0 不含任何应用概念**。几何、路由、进程模型——应用完全生长在语义层之上。这两层对应通信设备商的可交付物，其生命周期以"永久"为目标。

---

## 5. L-1 时空数学层

### 5.1 职责

只回答一个问题：**"事件该路由给谁？"** 不计算、不模拟、不裁决。

### 5.2 三大构件

1. **时空坐标系**：支持多尺度（城市级到毫米级）空间定位，层次化索引结构，索引键为多尺度坐标编码。
2. **时空索引**：判定时空邻近，输出"可能产生感知关系的实体对"候选集。支持海量动态实体的高频移动更新。
3. **关系索引（组织/社交图）**：判定实体间是否存在关系边（同事、团队、设备归属、订阅关注）。与时空索引并列，输出同一种判定："这两个实体应该收到彼此的事件"。

### 5.3 两条感知通道

| | 通道 A：时空通道 | 通道 B：关系通道 |
|---|---|---|
| 触发 | 时空邻近 | 关系图边 |
| 性质 | 对称、匿名、临时、被动 | 可不对称、实名、持久、主动 |
| 带宽 | 随距离衰减 | 由关系强度决定，与距离无关 |

### 5.4 关系建立的迟滞机制（双阈值）

关系的建立与撤销必须使用双阈值迟滞，防止实体在边界往复导致关系频繁震荡：

```
建立阈值：较小范围 r_enter —— 进入才建立订阅/升级代理/触发加密更新
撤销阈值：较大范围 r_exit（典型 1.3–2.0 倍）—— 离开才撤销/降级
附加时间迟滞：持续越线 N 个采样周期才切换
```

该机制统一适用于：时空通道订阅建撤、代理质量分级切换、交互精度升级。迟滞参数是纯几何量，属 L-1 职责。

---

## 6. L0 传输层

### 6.1 事件信封

```
Envelope {
    protocol:  AppProtocolId + 版本   // 应用协议标识（接收方据此选择解释器）
    from:      EntityId
    channel:   ChannelId              // 所属通道（时空/关系/私有）
    stamp:     发送方局部时间戳         // 仅用于接收方对单一发送者流的重建排序
    payload:   opaque bytes           // 网络层永不打开
}
```

### 6.2 传输承诺

- 按数据画像分流：高频连续量走尽力通道（可丢、只要最新）；事实事件走可靠有序通道（必达）；表现性内容最低优先级（可整体丢弃）。
- 通道内按序、完整性校验、不可篡改。

### 6.3 应用协议（Protocol Profiles）

- 每类事件是一种应用协议，独立 ID 与版本，为端点间双边约定，网络无感知。新应用 = 新协议，**基础设施零改动**。
- 实体建立关系时交换协议能力表；不认识的协议可降级忽略或走通用兜底呈现。
- 协议版本滚动升级，新旧版本事件同通道共存。

> 对标准组织的含义：基础设施只需要标准化**信封格式与索引接口**；应用协议由各垂直行业自行定义，如同 HTTP 不定义网页内容。

---

## 7. L1 实体层

### 7.1 实体模型

每个实体是运行于服务侧（含边缘节点）的独立进程：自主更新周期、时间线环形缓冲（状态历史）、事件收发队列、协议能力表。

### 7.2 更新节奏自适应

- 基础节奏由实体类型决定；修正因子：被观察密度↑ → 节奏↑；交互烈度↑ → 节奏↑；无观察者 → 沉睡（索引保留条目，事件唤醒）。
- **区分两个频率**：实体自身模拟节奏（本地恒定）与面向每个订阅者的**发布频率**（按订阅级别逐订阅节流）。大型活动中远处代理的低频呈现属后者，不影响主体自身运转。

### 7.3 交互的三级处理：协同是例外，路由是本体

**网络的最关键职能是路由，不是仲裁。** 实体运行在受信的服务侧，其业务结算结果天然权威：

```
Level 0 结果直送（绝大多数交互）
  主体 A 的服务侧实体本地结算 → 结果事件即事实 → 直接路由给 B
  → B 不重复计算：接收、应用、呈现

Level 1 规则消解（冲突但可交换化，少量）
  时间窗内双方动作冲突 → 路由节点上的静态规则直接消解
  （无状态纯函数、可配置，不构成裁决过程）

Level 2 临时协同（强耦合，极少数）
  毫秒级互相决定的场景（远程手术、协同操控、对抗性体育）
  → 临时建立协同上下文，交互结束即销毁
```

事实的定义：**第一个成立的结果事件即为事实**——大部分事实由实体自己书写，网络侧只负责保管与对齐。事实一旦成立即终局。

### 7.4 信任模型

- 所有实体运行于受信基础设施：接收方信任的不是"对方的终端"，而是"对方实体的服务侧输出"。
- 终端只是观众：只发送输入意图，收到的只是呈现脚本。
- **未订阅的实体，其事件不下发**——信息暴露面在架构上最小化，隐私与防窃听获得结构性保障（而非依赖应用层自觉）。

---

## 8. 会话域：一人一个体验现场

### 8.1 域的结构

```
SessionDomain {
    owner: 用户实体（域内唯一一等主体，最高节奏优先级）
    agents: 在场代理列表 = 我订阅的一切
        ├─ 真人代理（其他用户，事件经网络路由）
        └─ 虚拟代理（数字人/设备镜像，服务侧编排驱动）
    localClock: 域内时钟（跟随主人的节奏）
    stage: 体验编排器（把事件重建为体验）
}
```

### 8.2 相遇即订阅

- 时空通道：感知范围重叠 → 互相创建代理，临时订阅建立（建撤遵循双阈值迟滞，5.4）。
- 关系通道：团队/同事/关注 → 持久订阅，跨地域同频。
- 代理质量分级：

| 级别 | 触发 | 数据内容 | 更新频率 |
|---|---|---|---|
| P0 占位 | 极远/列表中 | 位置 + 外观摘要 | 秒级 |
| P1 轮廓 | 远距可见 | 位置重建 + 基础动作 | 1–5Hz |
| P2 完整 | 近距/交互中 | 完整状态 + 运动导数 | 10–20Hz |
| P3 同步 | 毫秒级强耦合 | 输入级同步 | 逐帧 |

P3 是"临时协同域"的唯一保留形态（远程精密操控等场景），交互结束即降级。**除 P3 外，本架构不存在域合并。**

### 8.3 交互闭环

```
1. A 对 B 的代理发起动作 → 意图上报 A 的服务侧实体
2. A 的实体本地结算（Level 0，无仲裁）→ 结果事件即事实，路由给 B
3. 事实下发：A 域呈现"动作生效"；B 域呈现对应结果
4. 两域各自呈现（允许百毫秒级呈现差），因果结论一致，永不合账
```

### 8.4 超大场景的消解

十万人级活动 = 每个用户的订阅列表变长，而非系统合并出巨型模拟体：

- 每用户域内：数万个代理按分级管理（P0 占绝大多数，成本趋零）；
- 网络侧：路由规模随订阅边数线性增长，配合分级节流可控；
- 无全局时钟屏障，算力随活跃度自然分布——**容量天花板在数学上不存在**。

---

## 9. 交互一致性规则：可交换化设计准则

### 9.1 立法原则

> 一切多主体交互的业务规则，必须能在"时序模糊 ±200ms"的前提下产出唯一且各方均认可的事实。写不出这种规则的应用设计，视为协议设计缺陷。

### 9.2 手法清单

| 手法 | 说明 |
|---|---|
| 双生效 | 双方动作都成立，顺序无关（对抗性场景双方结果并行成立） |
| 偏袒发起者 | 发起方本地观察到的结果优先成立 |
| 宽限窗口 | 时序边界模糊化处理，边界附近按宽容规则结算 |
| 事后补偿 | 一方状态变更不作废其已发出的动作 |
| 资源复制 | 消灭互斥资源（并发领取、个人配额） |
| 声明-结算分离 | 动作先发声明、延迟结算，收编网络延迟 |

### 9.3 一致性的重定义

一致性 ≠ 裁决出客观先后（物理上不存在）。一致性 = **规则对时序模糊持对称态度，各方体验均不被推翻**。这对跨地域协同（亚欧美三地的延迟差不可消除）是架构级的解法，而非工程级的妥协。

---

## 10. 时空连续事件检测

### 10.1 问题

实体各有时间线，"同一时刻两物体在哪"无定义。碰撞/干涉/相遇检测从三维几何变为**时空几何**：对双方的运动扫掠体求交，相交时刻为解出的未知数。

### 10.2 三级检测

| 级 | 内容 | 用途 |
|---|---|---|
| L0 粗筛 | 扫掠包围体时空重叠测试（时空索引，键 = x,y,z,t） | 否掉 99% 实体对 |
| L1 轨迹求交 | 扫掠体解析求解相交时刻 | 移动避障、近距交互 |
| L2 升频精判 | 触发订阅级升频后重判 | 精密协同、安全关键判定 |

**交互烈度决定事件频率下限**：进入"扫掠体可能相交"关系即触发订阅级升频（遵循双阈值迟滞）。触发条件是"干涉可能性"而非单纯距离。

### 10.3 应用示例

车路协同（车辆与路侧单元互为代理，接近即升频）、无人机空域管理、机器人共线作业——这些场景天然是"自主节奏实体 + 时空订阅 + 干涉升频"的结构。

---

## 11. 呈现层：终端把事件重建为体验

### 11.1 终端模型

终端 = 以刷新率为节奏的订阅者。每帧：收事件 → 归入各实体时间线 → 重建视图 → 补间平滑 → 翻译为视听指令 → 呈现。

### 11.2 补间与时间

- **补间 = 用延迟换连续性**：终端呈现的是 t−Δ 时刻的状态，Δ = 采样间隔 + 抖动缓冲。
- **Δ 逐实体分化**：画面/体验是一幅"时间纵深图"——近处实体呈现 60ms 前，远处 6 秒前，静态设施呈现"永恒"。与物理视觉经验同构，用户无感。
- **Δ 自适应呼吸**：缓冲将耗尽则快速扩，充裕则缓慢缩。宁多延迟，不要抖动。
- **运动导数补偿稀疏采样**：事件协议须携带速度/加速度，低频实体经样条重建为流畅运动。

### 11.3 体验修复

事件乱序/迟到/丢失为常态，终端按策略栈处理：小偏差无视、中偏差平滑收敛、大偏差硬切并表现掩盖、断档外推或冻结。总原则：**体验连续性 > 数据准确性；已呈现不可推翻 > 呈现及时性。**

### 11.4 终端侧分层

```
R0 事件接入（应用中立，入基础设施）：订阅管理、时间线重建、乱序修复
R1 语义翻译（应用协议绑定）：事件 → 体验编排
R2 资产执行（引擎绑定）：渲染、音频、空间计算
```

资产 = 慢事件：模型/场景等大体量内容是节奏极慢的实体事件，加载复用事件通道（首包全量 + 增量）。

---

## 12. 编排层与网络运营商角色

### 12.1 服务侧职责（按比重排序）

- **路由（本体）**：事件路由——通道管理、订阅匹配、分级节流、带宽分配。绝大部分算力在此；
- **实体宿主**：承载实体进程的运行环境，业务结算在此完成（结算即事实）；
- **编排（按需）**：驱动虚拟代理、编排动态事件、缺席期间的世界补全；
- **规则消解（少量）**：路由路径上的静态冲突规则；
- **临时协同（极少数）**：仅 P3 强耦合场景，用完即毁。

### 12.2 预演与代理质量

时空索引预测两个主体将接触 → 提前数秒在各自域中生成对方代理开始预演 → 正式订阅建立时无缝接管。代理的预演质量决定"相遇"的无感度。

### 12.3 惰性计算与不可证伪

无观察者区域的实体沉睡或统计级模拟。计算预算从"诚实"转移到"与观察者已有认知不矛盾"。**用户已有认知优先级高于世界状态**：用户记得的事必须保持为真；无人知晓的事可自由改写。事实档案（记录、回放、审计）= 固化的认知，是全系统必须遵守的部分。

---

## 13. 多租户联邦：空间互联网的形态

### 13.1 定位

单个应用是"一个编排者给很多用户编排体验"；空间互联网是"**无数编排者给无数用户编排体验，共享同一套索引、路由与事实档案**"。

### 13.2 三个扩展部件

**① 身份协议（跨应用的"我"）**
用户携带身份与数字资产穿越不同应用空间。身份/外观/资产从应用协议升级为核心协议，所有应用空间必须实现其解释器（允许分级降级呈现）。

**② 产权档案（金融级记录）**
事实档案的子集，承载数字资产与交易的归属。持久性、不可篡改性提升至金融级（多副本、可验证签名、公开可审计）。**这是全系统唯一允许强一致的部件**——其正当性来自产权变更的低频特性，不冲击架构其余部分。资产归属只由产权档案回答，任何应用无权自判。

**③ 多租户联邦**
- 租户租用的不是服务器，是**发布事件的权利**：索引空间键前缀 + 路由配额 + 档案写入配额；
- 基础设施按多租户设计：键前缀隔离、路由计费、带宽弹性；
- 租户在其空间内拥有编排权，但必须遵守系统公理（事实终局、已呈现不可推翻、产权档案唯一权威）；
- 跨租户流动 = 协议能力表切换 + 订阅迁移，基础设施无感；
- 治理分工：平台治理协议（类似 ICANN/骨干网），租户治理内容。

### 13.3 与 6G/边缘计算的对接关系

- **边缘节点 = 实体的天然宿主**：实体进程贴近用户部署，发布节流与迟滞机制天然降低回传带宽需求；
- **分级订阅 = 网络切片的应用层表达**：P0–P3 订阅级别可直接映射为差异化的 QoS/切片等级；
- **时空索引 = 新型网络功能（NF）**：可作为独立网元进入核心网/边缘云架构，与 UPF 并列——"在场路由功能（PRF, Presence Routing Function）"；
- **事件信封 = 可标准化的最小协议面**：如同 IP 报头，信封是基础设施唯一需要全行业对齐的格式。

---

## 14. 目标场景与参数化验证

架构成立与否，以能否不改 L-1/L0/L1 装配出以下场景为准：

| 场景 | 关键参数 |
|---|---|
| 城市数字孪生 | 亿级实体，99.9% 沉睡，事件唤醒，P0 代理为主 |
| 十万人实时活动 | 代理发布频率分级至 1/6Hz 档、规则层限制群体级联交互、表现粗粒度化 |
| 远程呈现/全息会议 | P2 代理为主、关系通道为主、呈现预算最大化 |
| 工业协同/远程操控 | P3 同步订阅（临时协同域），逐帧输入级交换，安全关键走可靠通道 |
| 车路协同 | 时空通道 + 干涉升频（第 10 章），毫秒级迟滞切换 |
| 记录审计场景（金融/司法） | 全交互 Level 0 结算并全量留痕，审计 = 档案重放 |

---

## 15. 实施路线

- **M0 最小细胞**：两实体 + 一通道 + 一协议，事件流通与重建闭环（约千行内核）。验收：重建误差可测量。
- **M1 代理交互闭环**：P0–P2 分级、Level 0 结果直送、规则消解器。验收：双方体验各自自洽、无回滚、乱序下事实唯一。
- **M2 呈现完整化**：Δ 呼吸、样条补间、修复策略栈。验收：1/6Hz 代理在 60Hz 终端上视觉流畅。
- **M3 密度压力**：时空索引、分级节流、节奏自适应，十万代理同域。验收：成本随代理数线性，P0 成本趋零。
- **M4 编排系统**：虚拟代理、预演、缺席补全。验收：用户盲测无法区分代理与真人接管瞬间。
- **M5 联邦**：双租户 + 身份穿越 + 产权一致。验收：两租户互不感知实现、身份跨域正确呈现、产权归属跨域一致、基础设施零改动。

### 全局验证指标

1. 重建误差分布（呈现 vs 真实，P50/P95/P99）；
2. Δ 调节频率（目标 < 0.1 次/实体/秒）；
3. 事实延迟（意图 → 事实下发）；
4. 回滚率（目标 = 0）；
5. 关系通道流量占比（网络健康度）。

---

## 16. 风险清单

| 风险 | 缓解 |
|---|---|
| 代理预演质量不足致"接管突变感" | 预演提前量 + M4 盲测验收 |
| 回溯级联（检测结果修正传播至第三方） | 可交换化立法，回滚率指标归零化 |
| 无全局时钟下的运维观测困难 | 观测旁路：实体输出统一度量事件流 |
| 应用开发者心智（习惯全局时钟编程） | 工具链翻译协议细节为业务语言 |
| 应用协议碎片化 | 协议注册表 + 核心协议集收敛（移动/交互/身份/社交 四种起步） |
| P3 滥用 | P3 成本审计，默认仅安全/精密关键场景开放 |

---

## 17. 边界声明

以下事项本架构不承诺解决，属运营与治理层：内容合规、跨租户经济结算与法币兑换、身份的现实世界映射（实名政策）。接口预留在身份协议与产权档案扩展字段中，协议本身保持中立。

---

## 18. 术语表

- **会话域（Session Domain）**：一个用户的私人体验现场，他是其中唯一一等主体。
- **在场代理（Presence Agent）**：域内除主人外的一切实体呈现，事件源为真人（路由）或服务侧（编排）。
- **事件（Event）**：实体间交换的带信封不透明字节流，系统的唯一流通物。
- **应用协议（Protocol Profile）**：一类事件的格式约定，端点私约，网络无感。
- **事实（Fact）**：由服务侧实体一次结算生成的终局记录，基础设施保管与对齐，各方域独立呈现，永不改写。
- **时间线（Timeline）**：实体的状态历史环形缓冲，服务检测、补间、留痕。
- **时间纵深图**：体验画面各实体处于不同过去时刻的现象（Δ 逐实体分化）。
- **三级参与**：订阅 → 同步（P3，临时协同域）→ 协同，成本逐级跳升。
- **可交换化**：使交互结果与顺序无关的规则设计手法总称。
- **不可证伪**：模拟的唯一约束——不得与用户已有认知矛盾。
- **在场路由功能（PRF）**：时空索引与事件路由作为独立网络功能的网元化提议。

---

*本文档推导自三十年大规模实时系统工程的实践：从统一时钟架构的技术资产与其天花板，到密度经济学与商业结构的经验，最终收敛为"一人一域"的在场路由架构。其每一条公理，都能在工程史中找到代价与证据。*
